# ai-rag 测试用例全量设计与验证报告

生成日期:2026-09-12 · 来源:对 `/Users/tomtong/Software/js/ai-rag` 的**只读**扫描(未改动该仓任何文件) · 用例总数 **223**

## 一、执行汇总

| 指标 | 值 |
| --- | --- |
| 用例总数 | 223 |
| 按优先级 | P0 102 · P1 88 · P2 33 |
| 按类型 | 异常 60 · 正向 57 · 边界 57 · 一致性 34 · 权限 15 |
| 实现核查 | 已实现 184 · 部分实现 29 · 存疑 9 · 未发现实现 1 |
| 现有验证承载 = 无(盲区) | **134**(占 60%) |

## 二、运行时实测结论(服务实跑,非静态阅读)

| 项 | 结论 | 证据 |
|---|---|---|
| 契约·鉴权 X-Service-Key | ✅ 通过 | 带头发 200;不带头 401 |
| 契约·camelCase fileContentBase64 | ✅ 通过 | 长文本 200 且 chunk_count=1 已入索引 |
| 契约·/rag/extract 存在 | ✅ 通过 | 空 body 返回 400(非 404),消息明写「ep 契约」 |
| 契约·namespace 隔离 | ✅ 通过 | battery 与 ep-docs 命中集合不同 |
| 契约·SSE 正常事件序 | ✅ 通过 | 实读事件序 meta → delta → delta |
| 契约·scopes 非空过滤 | ⏳ 未测 | 仅传了空数组 |
| allowed_security_levels: [] | ⚠️ 确认 | 实测与不传等价(均不过滤);风险取决于调用方预期 |
| 垃圾 namespace 静默回退 | ❌ 否定 | 实测返回 0 条,未回退(author 代码推断被推翻) |
| 脱敏正则误伤 document_id | 🔴 确认 | 36 位 UUID 中段被替换为 [已脱敏:phone],产出残缺 ID |
| 短文档入库 chunk_count=0 仍报 ok | ⚠️ 确认 | 564B → chunk_count=1;18B → 0 但 ok:true |
| 网关只发 X-API-Key | ⚠️ 确认 | api/src/services/rag/http.ts:24;ep_mode 只对直连生效 |

## 三、测试盲区清单(现有验证承载 = 无)

- **检索**(17):TC-RAG-RET-006、TC-RAG-RET-007、TC-RAG-RET-008、TC-RAG-RET-009、TC-RAG-RET-010、TC-RAG-RET-011、TC-RAG-RET-012、TC-RAG-RET-013、TC-RAG-RET-014、TC-RAG-RET-016、TC-RAG-RET-017、TC-RAG-RET-018、TC-RAG-RET-019、TC-RAG-RET-020、TC-RAG-RET-021、TC-RAG-RET-022、TC-RAG-RET-025
- **流式-ep**(10):TC-RAG-CHAT-037、TC-RAG-CHAT-038、TC-RAG-CHAT-039、TC-RAG-CHAT-045、TC-RAG-CHAT-046、TC-RAG-CHAT-048、TC-RAG-CHAT-049、TC-RAG-CHAT-050、TC-RAG-CHAT-051、TC-RAG-CHAT-067
- **入库**(10):TC-RAG-RET-043、TC-RAG-RET-045、TC-RAG-RET-046、TC-RAG-RET-047、TC-RAG-RET-048、TC-RAG-RET-050、TC-RAG-RET-053、TC-RAG-RET-054、TC-RAG-RET-055、TC-RAG-RET-059
- **流式-存量**(9):TC-RAG-CHAT-027、TC-RAG-CHAT-028、TC-RAG-CHAT-029、TC-RAG-CHAT-030、TC-RAG-CHAT-032、TC-RAG-CHAT-033、TC-RAG-CHAT-034、TC-RAG-CHAT-035、TC-RAG-CHAT-060
- **/chat 非流式**(7):TC-RAG-CHAT-003、TC-RAG-CHAT-004、TC-RAG-CHAT-006、TC-RAG-CHAT-007、TC-RAG-CHAT-008、TC-RAG-CHAT-011、TC-RAG-CHAT-068
- **LLM 客户端**(7):TC-RAG-CHAT-020、TC-RAG-CHAT-021、TC-RAG-CHAT-022、TC-RAG-CHAT-023、TC-RAG-CHAT-024、TC-RAG-CHAT-025、TC-RAG-CHAT-026
- **引用**(6):TC-RAG-CHAT-040、TC-RAG-CHAT-041、TC-RAG-CHAT-042、TC-RAG-CHAT-043、TC-RAG-CHAT-044、TC-RAG-CHAT-047
- **一致性**(6):TC-RAG-CHAT-057、TC-RAG-CHAT-058、TC-RAG-CHAT-059、TC-RAG-CHAT-061、TC-RAG-RET-071、TC-RAG-RET-072
- **/diagram/generate**(6):TC-RAG-EXT-033、TC-RAG-EXT-034、TC-RAG-EXT-035、TC-RAG-EXT-036、TC-RAG-EXT-039、TC-RAG-EXT-042
- **/knowledge/graph/plan**(6):TC-RAG-EXT-044、TC-RAG-EXT-045、TC-RAG-EXT-046、TC-RAG-EXT-047、TC-RAG-EXT-048、TC-RAG-EXT-049
- **/knowledge-gaps/cluster-drafts**(6):TC-RAG-EXT-054、TC-RAG-EXT-055、TC-RAG-EXT-057、TC-RAG-EXT-058、TC-RAG-EXT-061、TC-RAG-EXT-062
- **会话与 history**(5):TC-RAG-CHAT-009、TC-RAG-CHAT-010、TC-RAG-CHAT-054、TC-RAG-CHAT-055、TC-RAG-CHAT-056
- **/artifacts/image/contract**(5):TC-RAG-EXT-022、TC-RAG-EXT-023、TC-RAG-EXT-027、TC-RAG-EXT-028、TC-RAG-EXT-029
- **重建索引**(5):TC-RAG-RET-061、TC-RAG-RET-062、TC-RAG-RET-063、TC-RAG-RET-064、TC-RAG-RET-065
- **文档列表**(5):TC-RAG-RET-066、TC-RAG-RET-067、TC-RAG-RET-068、TC-RAG-RET-069、TC-RAG-RET-070
- **检索调试**(4):TC-RAG-RET-036、TC-RAG-RET-037、TC-RAG-RET-039、TC-RAG-RET-040
- **usage 纪律**(3):TC-RAG-CHAT-005、TC-RAG-CHAT-018、TC-RAG-CHAT-019
- **/extract**(3):TC-RAG-EXT-010、TC-RAG-EXT-011、TC-RAG-EXT-014
- **检索层**(3):TC-RAG-RET-075、TC-RAG-RET-076、TC-RAG-RET-078
- **/health**(2):TC-RAG-CHAT-062、TC-RAG-CHAT-063
- **/usage**(2):TC-RAG-CHAT-064、TC-RAG-CHAT-065
- **/documents**(2):TC-RAG-EXT-066、TC-RAG-EXT-067
- **/chat**(2):TC-RAG-EXT-073、TC-RAG-EXT-074
- **/terminology/contract**(1):TC-RAG-EXT-065
- **/search/debug**(1):TC-RAG-EXT-072
- **scopes**(1):TC-RAG-RET-032


## 四、全量用例

| 用例编号 | 模块 | 用例标题 | 优先级 | 类型 | 前置条件 | 测试数据 | 操作步骤 | 预期结果 | 现有验证承载 | 实现核查 | 核查说明 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-RAG-CHAT-001 | /chat 非流式 | 正常问答返回 answer 与 sources | P0 | 正向 | 未配置 DEEPSEEK_API_KEY（走 mock 回退）；知识库已索引电池语料 | {"query":"绝缘电阻测试的合格阈值是多少？","top_k":5} | 1) POST /rag/chat 2) 解析响应 JSON 3) 校验字段 | HTTP 200；answer 非空字符串；len(sources)>=1；0<=confidence<=1；answer_ir.status 属于 answered 或 partial；trace.retrieval_ms>=0 | 无（仅 pipeline 级 test_quality_eval_parameter_answer_confidence_and_answer_ir；api/tests/test-chat.sh 打的是网关 /api/chat） | 已实现 | routes.py:370 定义 /chat，但 rag/tests 内无任何 HTTP 级 /rag/chat 调用 |
| TC-RAG-CHAT-002 | /chat 非流式 | question 别名兼容 query | P0 | 正向 | 同上 | {"question":"OCV 测试合格范围是多少？"} | 1) 用 question 字段 POST /rag/chat 2) 读取 answer_ir.query_rewrite | HTTP 200；answer_ir.query_rewrite.original_query 等于提交的问题文本 | test_capability_contract.py::test_chat_request_accepts_ep_shape_aliases | 已实现 | models.py:231 用 AliasChoices("query","question")；该测试只验模型解析，未打路由 |
| TC-RAG-CHAT-003 | /chat 非流式 | 缺少 query 字段 | P0 | 边界 | 无 | {"top_k":5} | 1) POST /rag/chat 2) 读取 422 响应体 | HTTP 422；detail[0].loc 含 "query"；detail[0].type 为 missing | 无 | 已实现 | ChatRequest.query 无默认值，Pydantic 直接拒绝 |
| TC-RAG-CHAT-004 | /chat 非流式 | 空字符串 question 触发低信息拒答 | P0 | 边界 | 无 | {"query":""} | 1) POST /rag/chat 2) 读取 answer 与 answer_ir | HTTP 200；answer 等于 "根据当前知识库信息，我暂时无法确认该问题。"；confidence 等于 0.0；answer_ir.status 等于 insufficient_context；answer_ir.metadata.refusal_reasons 含 "low_information" | 无 | 已实现 | models.py:231 无 min_length；rewrite.py:654 对空串判定 low_information，routes.py:384 不为此抛错 |
| TC-RAG-CHAT-005 | usage 纪律 | 超长 question 触发请求级预算 | P0 | 异常 | 配置有效 DEEPSEEK_API_KEY；LLM_MAX_INPUT_CHARS_PER_REQUEST 为 25000 | query 为 30000 个中文字符 | 1) POST /rag/chat 2) 读取 429 响应体 | HTTP 429；body.detail.code 等于 "LLM_BUDGET_EXCEEDED"；body.detail.limit_type 等于 "request_input_chars"；body.detail.limit 等于 25000 | 无 | 已实现 | usage_guard.py:83 抛 LlmBudgetExceeded，routes.py:384 转 429；未配 key 走 mock 则不触发 |
| TC-RAG-CHAT-006 | /chat 非流式 | top_k 下界越界 | P0 | 边界 | 无 | {"query":"绝缘电阻测试","top_k":0} | 1) POST /rag/chat 2) 读取校验错误 | HTTP 422；detail[0].loc 含 "top_k"；detail[0].type 为 greater_than_equal | 无 | 已实现 | models.py:232 Field(ge=1) |
| TC-RAG-CHAT-007 | /chat 非流式 | top_k 上界越界 | P0 | 边界 | 无 | {"query":"绝缘电阻测试","top_k":51} | 1) POST /rag/chat 2) 读取校验错误 | HTTP 422；detail[0].loc 含 "top_k"；detail[0].type 为 less_than_equal | 无 | 已实现 | models.py:232 Field(le=50) |
| TC-RAG-CHAT-008 | /chat 非流式 | top_k 取上界 50 | P1 | 边界 | 知识库命中充足 | {"query":"绝缘电阻测试","top_k":50} | 1) POST /rag/chat 2) 统计 sources 长度 | HTTP 200；len(sources)<=50；trace.retrieval_ms>=0 | 无 | 已实现 | pipeline.search 内 candidate_top_k 上限 30 后再 rerank 截断到 top_k |
| TC-RAG-CHAT-009 | 会话与 history | history 为空数组 | P1 | 正向 | 无 | {"query":"绝缘电阻测试标准是什么？","history":[]} | 1) POST /rag/chat 2) 读取 answer_ir.query_rewrite | HTTP 200；answer_ir.query_rewrite.history_turns 等于 0；answer_ir.query_rewrite.strategy 不等于 history_pronoun_resolution | 无 | 已实现 | rewrite.py:626 _count_history_turns 对空列表返回 0 |
| TC-RAG-CHAT-010 | 会话与 history | history 超长仅取末段入 prompt | P1 | 边界 | 构造 200 轮对话 | history 为 200 组 role 与 content 交替 | 1) POST /rag/chat 2) 断言 history_turns 3) 检查 prompt 溢出 | HTTP 200；answer_ir.query_rewrite.history_turns 等于 200；不返回 5xx；请求体未被截断拒绝 | 无 | 已实现 | prompt_builder.py:91 只取 history[-6:]；rewrite.py:633 只扫 history[-8:]；无长度上限校验 |
| TC-RAG-CHAT-011 | /chat 非流式 | stream=true 仍返回 JSON | P1 | 边界 | 无 | {"query":"绝缘电阻测试标准是什么？","stream":true} | 1) POST /rag/chat 2) 读取 Content-Type | HTTP 200；Content-Type 为 application/json；响应体可被 json.loads 解析；非 text/event-stream | 无 | 部分实现 | ChatRequest.stream 字段在 routes.py:373 与 453 均未被读取，属声明但无效字段 |
| TC-RAG-CHAT-012 | 检索边界 | namespace 不存在时静默建空集合并拒答 | P0 | 边界 | 无 | {"query":"绝缘电阻测试标准是什么？","namespace":"ns-not-exist-xyz"} | 1) POST /rag/chat 2) 读取 answer 与 sources | HTTP 200；answer 等于 REFUSAL_ANSWER；sources 为空数组；answer_ir.metadata.refusal_reasons 含 "no_valid_citations" | 部分：test_capability_contract.py::test_namespace_resolution_ep_docs_and_explicit_mapping（只验名字解析） | 已实现 | vector_store.py:74 用 get_or_create_collection，未知 namespace 自动建空集合不报错 |
| TC-RAG-CHAT-013 | 拒答 | 低信息问题不调用 LLM | P0 | 异常 | 无 | {"query":"这个呢？","top_k":3} | 1) POST /rag/chat 2) 读取 trace 与 refusal_reasons | HTTP 200；answer 等于 REFUSAL_ANSWER；trace.llm_ms 等于 0；answer_ir.metadata.refusal_reasons 含 "low_information" | test_refusal_policy.py::test_chat_refuses_low_information_query_without_llm 与 test_quality_eval_suite.py::test_quality_eval_refusal_for_low_information_query | 已实现 | 两个测试用 monkeypatch 断言 LLM 不被调用，但均为 pipeline 级非 HTTP 级 |
| TC-RAG-CHAT-014 | 拒答 | 弱证据拒答并给出结构化原因 | P0 | 异常 | 命中的 top1 score 使 confidence 低于 0.5 | {"query":"绝缘电阻测试标准是什么？","top_k":1} | 1) POST /rag/chat 2) 读取 refusal_reasons 与 trace | HTTP 200；answer_ir.status 等于 insufficient_context；refusal_reasons 含 "insufficient_evidence"；trace.generate_ms 等于 0 | test_refusal_policy.py::test_chat_refuses_weak_evidence_with_structured_reason | 已实现 | refusal.py:45 以 MIN_ANSWER_CONFIDENCE 0.5 为阈值 |
| TC-RAG-CHAT-015 | 引用 | 拒答时 confidence 归零且 claims 为空 | P1 | 异常 | sources 非空但答案命中拒答短语 | {"query":"绝缘电阻测试异常怎么处理？"} | 1) POST /rag/chat 2) 读取 answer_ir 3) 校验 citations 与 claims | answer_ir.status 等于 insufficient_context；answer_ir.confidence 等于 0.0；answer_ir.claims 为空数组；answer_ir.citations 非空 | test_answer_ir.py::test_answer_ir_refusal_phrase_zeroes_answer_confidence_with_sources | 已实现 | models.py:349 拒答状态强制 answer_confidence 为 0 |
| TC-RAG-CHAT-016 | 权限 | 未配置 RAG_API_KEY 时不校验鉴权头 | P0 | 权限 | RAG_API_KEY 为空 | GET /rag/usage 无任何鉴权头 | 1) 置空 RAG_API_KEY 2) GET /rag/usage 3) 读取状态码 | HTTP 200 | test_capability_contract.py::test_auth_skipped_when_key_unconfigured | 已实现 | routes.py:78 verify_api_key 在未配置密钥时直接 return |
| TC-RAG-CHAT-017 | 权限 | 已配置 RAG_API_KEY 时服务键校验 | P0 | 权限 | RAG_API_KEY 为 cap-test-key | 请求头分别为 X-Service-Key 正确、X-API-Key 正确、错误值、缺失 | 1) 分别以四种请求头 POST /rag/chat 2) 读取状态码 | 两种正确头均 200；错误值与缺失均 401；401 body.detail 等于 "Invalid or missing API key" | test_capability_contract.py::test_auth_accepts_service_key_and_legacy_key | 已实现 | 该测试样本端点为 /rag/usage，未覆盖 /rag/chat 与 /rag/chat/stream |
| TC-RAG-CHAT-018 | usage 纪律 | 用户小时配额耗尽返回 429 | P0 | 异常 | 配置有效 key；LLM_HOURLY_REQUEST_LIMIT_PER_USER 为 1；usage 日志当天已有 1 条同用户成功记录 | {"query":"绝缘电阻测试标准是什么？"} | 1) POST /rag/chat 2) 读取 429 响应体 | HTTP 429；body.detail.code 等于 "LLM_BUDGET_EXCEEDED"；body.detail.limit_type 等于 "user_hourly_requests"；body.detail.used 等于 1 | 无 | 已实现 | usage_guard.py:110 按小时桶统计；该分支无任何测试 |
| TC-RAG-CHAT-019 | usage 纪律 | mock 模式不写用量也不校验预算 | P0 | 异常 | 未配置 DEEPSEEK_API_KEY | 先 GET /rag/usage 记录 success_requests 与 input_chars，再连续 POST /rag/chat 三次 | 1) 取基线 2) 三次问答 3) 再取 /rag/usage | 三次均 HTTP 200；success_requests 与 input_chars 与基线一致；无 429 | 无 | 部分实现 | client.py:57 与 122 在 mock_reason 非空时提前返回，绕过 check_budget 与 record_usage |
| TC-RAG-CHAT-020 | LLM 客户端 | LLM_PROVIDER=mock 强制降级 | P1 | 异常 | LLM_PROVIDER=mock | {"query":"绝缘电阻测试标准是什么？"} | 1) 设 LLM_PROVIDER=mock 2) POST /rag/chat 3) 读取 answer 前缀 | HTTP 200；answer 以 "[Mock LLM - LLM_PROVIDER=mock]" 开头 | 无 | 已实现 | client.py:12 _OFFLINE_PROVIDERS 含 mock/offline/none/disabled |
| TC-RAG-CHAT-021 | LLM 客户端 | 无 key 走 mock 回退并标注原因 | P0 | 异常 | DEEPSEEK_API_KEY 为空；LLM_PROVIDER=deepseek；检索有命中 | {"query":"绝缘电阻测试标准是什么？"} | 1) 清空 key 2) POST /rag/chat 3) 读取 answer 首行 | HTTP 200；answer 以 "[Mock LLM - DEEPSEEK_API_KEY is empty]" 开头；answer 含至少一条上下文条目 | 无 | 已实现 | client.py:36 _mock_reason 返回 "DEEPSEEK_API_KEY is empty"；client.py:206 拼装前缀 |
| TC-RAG-CHAT-022 | LLM 客户端 | 不支持的 provider 降级 | P2 | 异常 | LLM_PROVIDER=azure | {"query":"绝缘电阻测试标准是什么？"} | 1) 设 LLM_PROVIDER=azure 2) POST /rag/chat 3) 读取 answer 前缀 | HTTP 200；answer 以 "[Mock LLM - unsupported LLM_PROVIDER=azure]" 开头 | 无 | 已实现 | client.py:41 provider 非 deepseek 时返回 unsupported 原因 |
| TC-RAG-CHAT-023 | LLM 客户端 | 上游 5xx 不伪造答案返回 500 | P0 | 异常 | 配置有效 key；DEEPSEEK_BASE_URL 指向固定返回 500 的桩服务 | {"query":"绝缘电阻测试标准是什么？"} | 1) POST /rag/chat 2) 读取状态码与响应体 | HTTP 500；body.detail 等于 "Internal server error"；响应体不含任何答案文本 | 无 | 已实现 | client.py:117 抛 LlmProviderError；routes.py:386 统一转 500 |
| TC-RAG-CHAT-024 | LLM 客户端 | 上游超时返回 500 | P0 | 异常 | 配置有效 key；上游接受连接后不返回数据超过 60 秒 | {"query":"绝缘电阻测试标准是什么？"} | 1) POST /rag/chat 2) 等待超时 3) 读取状态码 | HTTP 500；body.detail 等于 "Internal server error"；服务端留有 DeepSeek API error 日志 | 无 | 已实现 | client.py:23 客户端 timeout=60.0 与 max_retries=2，超时后转 LlmProviderError |
| TC-RAG-CHAT-025 | LLM 客户端 | 上游不可达返回 500 | P1 | 异常 | 配置有效 key；DEEPSEEK_BASE_URL 指向不可路由地址 | {"query":"绝缘电阻测试标准是什么？"} | 1) POST /rag/chat 2) 读取状态码 | HTTP 500；body.detail 等于 "Internal server error" | 无 | 已实现 | client.py:106 APIConnectionError 被兜底捕获 |
| TC-RAG-CHAT-026 | LLM 客户端 | mock-key 占位分支不可达 | P2 | 异常 | 未配置 key 且 LLM_PROVIDER 为 deepseek | 直接调用 client._mock_reason() 与 client._get_client() | 1) 清空 key 2) 断言 _mock_reason() 非空 3) 断言 chat 走 mock 分支 | _mock_reason() 返回非空字符串，_get_client() 在该配置下永不被调用；api_key 的 "mock-key" 兜底为死代码 | 无 | 存疑 | client.py:20 的 or "mock-key" 与 client.py:59 的提前 return 互斥，无测试证明该分支可达 |
| TC-RAG-CHAT-027 | 流式-存量 | 正常问答事件序 stage 到 done | P0 | 正向 | 不带 X-Service-Key 头 | {"query":"绝缘电阻测试标准是什么？","top_k":5} | 1) POST /rag/chat/stream 2) 按行解析 data: 前缀 3) 抽取各事件 type | HTTP 200；事件 type 序列为 stage、meta、token 出现 1 次以上、done；首个事件 type 等于 "stage"；不含 delta 与 citations 事件 | 无 | 已实现 | routes.py:471 与 499 决定存量序；rag/tests 内仅 test_refusal_policy.py:213 一处调用该端点 |
| TC-RAG-CHAT-028 | 流式-存量 | stage 事件字段固定 | P1 | 正向 | 同 TC-RAG-CHAT-027 | 同 TC-RAG-CHAT-027 | 1) POST /rag/chat/stream 2) 取首个事件对象 | 首个事件等于 {"type":"stage","stage":"accepted","message":"RAG 流式请求已接收。"} | 无 | 已实现 | routes.py:472 字面量 |
| TC-RAG-CHAT-029 | 流式-存量 | meta 事件携带检索元数据 | P0 | 正向 | 同 TC-RAG-CHAT-027 | 同 TC-RAG-CHAT-027 | 1) POST /rag/chat/stream 2) 取第二个事件对象 | 第二个事件 type 等于 "meta"；含 retrieval_ms 整数、hit_count 整数、query_rewrite 对象；query_rewrite 含 strategy 与 rewritten_query | 无 | 已实现 | routes.py:499 存量 meta 字段集 |
| TC-RAG-CHAT-030 | 流式-存量 | done 事件携带完整回答载荷 | P0 | 正向 | 同 TC-RAG-CHAT-027 | 同 TC-RAG-CHAT-027 | 1) POST /rag/chat/stream 2) 取最后一个事件对象 | 末事件 type 等于 "done"；含 sources 数组、confidence 数值、followups 数组、answer_ir 对象、visual_plan 对象；answer_ir 含 status、citations、warnings | 无 | 已实现 | routes.py:627 done_data 六个键 |
| TC-RAG-CHAT-031 | 流式-存量 | 零命中走流式拒答 | P0 | 异常 | 检索零命中且 query 非低信息 | {"query":"量子计算机散热方案对比","top_k":5} | 1) POST /rag/chat/stream 2) 解析事件序 3) 取 done 事件 | 事件序为 stage、meta（hit_count 等于 0）、token（content 等于 REFUSAL_ANSWER）、done；无 error 事件；done.answer_ir.status 等于 insufficient_context | test_refusal_policy.py::test_stream_chat_emits_structured_refusal_without_llm | 已实现 | 该测试用 query "这个呢？" 走的是 low_information 拒绝，零命中分支未被单独覆盖 |
| TC-RAG-CHAT-032 | 流式-存量 | 下游 LLM 失败透传 error 且无 done | P0 | 异常 | 配置有效 key；上游返回 5xx | {"query":"绝缘电阻测试标准是什么？"} | 1) POST /rag/chat/stream 2) 解析事件序 3) 检查 error 事件字段 | 事件序为 stage、meta、error；error 载荷为上游原始形态 {"type":"error","message":...} 且不含 code 字段；error 之后无 done 事件 | 无 | 已实现 | routes.py:636 对非 token 非 done 的事件走 else 透传，与 ep 模式规范化行为不一致 |
| TC-RAG-CHAT-033 | 流式-存量 | 检索异常以 error 事件收尾 | P0 | 异常 | Chroma 持久化目录不可读，使 search 抛异常 | {"query":"绝缘电阻测试标准是什么？"} | 1) POST /rag/chat/stream 2) 解析事件序 3) 读取 error.code | HTTP 200；事件序为 stage、error；error.code 等于 "INTERNAL_ERROR"；无 meta 与 done | 无 | 已实现 | routes.py:639 兜底 except 生成 INTERNAL_ERROR；HTTP 状态仍是 200 |
| TC-RAG-CHAT-034 | 流式-存量 | mock 回退的 token 分片与自造 done | P1 | 异常 | 未配置 key；LLM_PROVIDER=deepseek | {"query":"绝缘电阻测试标准是什么？"} | 1) POST /rag/chat/stream 2) 统计 token 事件数量 3) 检查 done 来源 | token 事件数量等于 ceil(len(mock 回答)/3)；done 事件含 sources 与 answer_ir（由路由自造，非上游 done 原文） | 无 | 已实现 | client.py:128 每 3 字符一片；routes.py:603 命中 done 后丢弃上游载荷 |
| TC-RAG-CHAT-035 | 流式-存量 | SSE 响应头与编码 | P1 | 正向 | 无 | {"query":"绝缘电阻测试标准是什么？"} | 1) POST /rag/chat/stream 2) 读取响应头与响应体文本 | Content-Type 含 text/event-stream；Cache-Control 等于 no-cache；Connection 等于 keep-alive；X-Accel-Buffering 等于 no；X-Request-Id 存在；响应体含未转义中文「根据当前知识库信息」 | 无 | 已实现 | routes.py:643 设置四个响应头；main.py:53 注入 X-Request-Id；ensure_ascii=False |
| TC-RAG-CHAT-036 | 权限 | 仅带 X-API-Key 时不进入 ep 模式 | P0 | 权限 | RAG_API_KEY 已配置为 cap-test-key | 请求头仅 X-API-Key: cap-test-key | 1) POST /rag/chat/stream 2) 解析事件序 | HTTP 200；首个事件 type 等于 "stage"；不存在 delta 与 citations 事件 | test_capability_contract.py::test_auth_accepts_service_key_and_legacy_key（只验鉴权通过） | 部分实现 | routes.py:461 ep_mode 只认 X-Service-Key，而 api/src/services/rag/http.ts:24 只发 X-API-Key，网关流量恒为存量序 |
| TC-RAG-CHAT-037 | 流式-ep | 正常问答事件序 meta 到 done | P0 | 正向 | 请求头带 X-Service-Key | {"namespace":"ep-docs","question":"外观检测的判定标准是什么？","history":[{"role":"user","content":"电芯分选工序包含哪些环节？"}]} | 1) POST /rag/chat/stream 带头 2) 解析事件 type 序列 | HTTP 200；事件 type 序列为 meta、delta 出现 1 次以上、citations、done；不含 stage 事件 | 无 | 已实现 | routes.py:496 起 ep 分支；rag/tests 与 api/tests 均无 ep 模式流式测试 |
| TC-RAG-CHAT-038 | 流式-ep | meta 事件字段集 | P0 | 正向 | 同 TC-RAG-CHAT-037 | 同 TC-RAG-CHAT-037 另加 session_id 为 "s-ep-1" | 1) POST /rag/chat/stream 带头 2) 取首个事件对象 | 首个事件 type 等于 "meta"；sessionId 等于 "s-ep-1"；messageId 等于 ""；不含 retrieval_ms、hit_count、query_rewrite | 无 | 部分实现 | routes.py:497 ep meta 只有两个键，检索耗时与命中数被丢弃，ep 侧无法观测检索质量 |
| TC-RAG-CHAT-039 | 流式-ep | delta 事件字段集 | P0 | 正向 | 同 TC-RAG-CHAT-037 | 同 TC-RAG-CHAT-037 | 1) POST /rag/chat/stream 带头 2) 遍历 delta 事件 | 每个 delta 事件恰含 type 与 text 两键；text 为非空字符串；不含 content 键 | 无 | 已实现 | routes.py:575 将上游 token 的 content 映射为 text |
| TC-RAG-CHAT-040 | 引用 | citations 事件 refs 四字段契约 | P0 | 正向 | 同 TC-RAG-CHAT-037；检索有命中 | 同 TC-RAG-CHAT-037 | 1) POST /rag/chat/stream 带头 2) 取 citations 事件 3) 校验 refs 元素键集 | citations 事件含 refs 数组且长度大于 0；每个元素的键集合恰为 docId、location、excerpt、inScope | 无 | 已实现 | routes.py:85 _ep_citation_refs 生产四字段；fixtures/ep_capability_requests.json:62 只声明契约无断言 |
| TC-RAG-CHAT-041 | 引用 | docId 取值与回退 | P0 | 正向 | 命中来源含 document_id | 检索命中 document_id 为 "doc-1"、chunk_id 为 "chunk-1" | 1) POST /rag/chat/stream 带头 2) 读 citations.refs[0].docId | refs[0].docId 等于 "doc-1"；当来源缺 document_id 时回退为 chunk_id；两者皆空时为空字符串 | 无 | 已实现 | routes.py:89 取值优先级 document_id 再 chunk_id |
| TC-RAG-CHAT-042 | 引用 | location 取值与页码回退 | P0 | 边界 | 命中来源 section_path 为空但 page_number 为 5 | 命中来源 {"section_path":"","page_number":5,"document_id":"doc-1"} | 1) POST /rag/chat/stream 带头 2) 读 citations.refs[0].location | section_path 非空时 location 等于 section_path；section_path 为空且 page_number 大于 0 时 location 等于 "第5页"；两者皆空时为空字符串 | 无 | 已实现 | routes.py:90 回退格式为 f"第{page_number}页" |
| TC-RAG-CHAT-043 | 引用 | excerpt 截断上限 | P0 | 边界 | 命中来源 snippet 长度为 500 字符 | 构造 snippet 为 500 个字符的来源 | 1) POST /rag/chat/stream 带头 2) 读 citations.refs 各元素 excerpt 长度 | 每个 excerpt 长度小于等于 200；excerpt 取自 snippet，snippet 为空时取自 content | 无 | 已实现 | routes.py:93 切片的 200 为硬编码，无配置项也无单测 |
| TC-RAG-CHAT-044 | 引用 | inScope 恒为 true | P0 | 边界 | 请求不做 scope 过滤 | {"namespace":"ep-docs","question":"外观检测的判定标准是什么？","scopes":[]} | 1) POST /rag/chat/stream 带头 2) 读全部 refs 的 inScope | 每个 ref 的 inScope 恒等于 true，即使 scopes 为空数组 | 无 | 部分实现 | routes.py:98 硬编码 True，不反映来源是否真正落在请求 scope 内 |
| TC-RAG-CHAT-045 | 流式-ep | done 事件仅含 usage | P0 | 正向 | 同 TC-RAG-CHAT-037 | 同 TC-RAG-CHAT-037 | 1) POST /rag/chat/stream 带头 2) 取末事件对象 | 末事件等于 {"type":"done","usage":""}；不含 sources、answer_ir、confidence | 无 | 部分实现 | routes.py:593 usage 恒为空字符串，ep 侧无法获得用量口径 |
| TC-RAG-CHAT-046 | 流式-ep | 零命中拒答事件序 | P0 | 异常 | namespace 为未知命名空间 | {"namespace":"ns-empty-xyz","question":"外观检测的判定标准是什么？"} | 1) POST /rag/chat/stream 带头 2) 解析事件序 3) 校验 delta 与 citations | 事件序为 meta、delta（text 等于 REFUSAL_ANSWER）、citations（refs 等于空数组）、done；无 error 事件 | 无 | 已实现 | routes.py:534 拒答分支 ep 输出 |
| TC-RAG-CHAT-047 | 引用 | 拒答但有命中时引用被丢弃 | P0 | 异常 | 命中存在但 confidence 低于 0.5 触发 insufficient_evidence | {"namespace":"ep-docs","question":"绝缘电阻测试标准是什么？","top_k":1} | 1) POST /rag/chat/stream 带头 2) 读 citations.refs 长度 3) 读 delta.text | delta.text 等于 REFUSAL_ANSWER；citations.refs 等于空数组，即使内部 sources 非空 | 无 | 部分实现 | routes.py:536 拒答分支硬编码 refs 为空数组，与成功分支的 _ep_citation_refs 不对称 |
| TC-RAG-CHAT-048 | 流式-ep | 下游 LLM 失败映射为规范化 error | P0 | 异常 | 配置有效 key；上游返回 5xx | {"namespace":"ep-docs","question":"外观检测的判定标准是什么？"} | 1) POST /rag/chat/stream 带头 2) 解析事件序 3) 读 error 字段 | 事件序为 meta、error；error.code 等于 "LLM_STREAM_ERROR"；error.message 非空；无 citations 与 done | 无 | 已实现 | routes.py:594 用 code 缺省值 LLM_STREAM_ERROR 规范化上游裸 error |
| TC-RAG-CHAT-049 | 流式-ep | 预算超限映射为 error 但丢字段 | P1 | 异常 | 配置有效 key；已触达 LLM_HOURLY_REQUEST_LIMIT_PER_USER | {"namespace":"ep-docs","question":"外观检测的判定标准是什么？"} | 1) POST /rag/chat/stream 带头 2) 读 error 事件键集 | 事件序为 meta、error；error.code 等于 "LLM_BUDGET_EXCEEDED"；error 不含 limit_type、limit、used、user_id | 无 | 部分实现 | usage_guard.py:23 as_detail 携带五个字段，routes.py:595 只保留 code 与 message |
| TC-RAG-CHAT-050 | 流式-ep | 检索异常时 meta 缺失 | P0 | 异常 | Chroma 不可用使 search 抛异常 | {"namespace":"ep-docs","question":"外观检测的判定标准是什么？"} | 1) POST /rag/chat/stream 带头 2) 解析事件序 | 首个事件即 error 且 code 等于 "INTERNAL_ERROR"；不存在 meta 事件；HTTP 状态仍为 200 | 无 | 部分实现 | routes.py:477 的 search 位于 496 的 meta 之前，异常时违反 EP-AI能力服务泛化蓝图.md 声明的 meta 优先事件序 |
| TC-RAG-CHAT-051 | 流式-ep | 未知上游事件不转发 | P2 | 边界 | 上游插入非 token、非 done、非 error 的事件 | 上游混入 {"type":"keepalive"} | 1) POST /rag/chat/stream 带头 2) 遍历响应全部事件 type | 响应中只出现 meta、delta、citations、done、error 五种 type；keepalive 不出现 | 无 | 已实现 | routes.py:597 continue 丢弃未知事件 |
| TC-RAG-CHAT-052 | 会话与 history | 指代消解影响检索 query 但不改 prompt 问题 | P0 | 正向 | 无 | query 为 "它的异常怎么处理？"；history 首轮为 "绝缘电阻测试标准是什么？" | 1) POST /rag/chat 2) 读 answer_ir.query_rewrite 与 query_understanding | 检索用 query 等于 "绝缘电阻测试标准的异常怎么处理？"；prompt 末条 user 内容等于 "它的异常怎么处理？"；strategy 等于 "history_pronoun_resolution" | test_query_rewrite.py::test_chat_uses_rewrite_trace_for_retrieval_and_answer_ir | 已实现 | 测试以 monkeypatch 捕获检索 query 与 prompt，属 pipeline 级 |
| TC-RAG-CHAT-053 | 会话与 history | 省略式追问补全主题 | P1 | 正向 | 无 | query 为 "合格阈值呢？"；history 首轮为 "绝缘电阻测试标准是什么？" | 1) POST /rag/chat 2) 读 answer_ir.query_rewrite | rewritten_query 等于 "绝缘电阻测试标准 合格阈值呢？"；strategy 等于 "history_ellipsis_completion"；signals 含 ellipsis | test_query_rewrite.py::test_rewrite_completes_elliptical_followup_from_recent_topic | 已实现 | 纯函数级测试，未验证经路由后仍生效 |
| TC-RAG-CHAT-054 | 会话与 history | 非对话角色不计入 history_turns | P1 | 边界 | 无 | history 为 [{"role":"system","content":"x"},{"role":"user","content":"绝缘电阻测试标准是什么？"}] | 1) POST /rag/chat 2) 读 answer_ir.query_rewrite.history_turns | history_turns 等于 1；system 角色条目被忽略 | 无 | 已实现 | rewrite.py:627 只计入 user 与 assistant 且 content 去空白后非空 |
| TC-RAG-CHAT-055 | 会话与 history | 非流式端点忽略 session_id | P1 | 边界 | 无 | {"query":"绝缘电阻测试标准是什么？","session_id":"s-1"} | 1) POST /rag/chat 2) 读响应顶层键 | HTTP 200；响应 message_id 等于 ""；响应不含 sessionId 与 session_id 字段 | 无 | 部分实现 | routes.py:373 未把 session_id 传入 pipeline，ChatResult 也无会话字段 |
| TC-RAG-CHAT-056 | 会话与 history | ep meta 回显 session_id 但 messageId 恒空 | P1 | 边界 | 请求头带 X-Service-Key | {"question":"外观检测的判定标准是什么？","session_id":"s-ep-9"} | 1) POST /rag/chat/stream 带头 2) 读 meta 事件 | meta.sessionId 等于 "s-ep-9"；meta.messageId 等于 "" | 无 | 部分实现 | routes.py:497 messageId 硬编码空串，ep 侧无法做消息级幂等 |
| TC-RAG-CHAT-057 | 一致性 | 同输入重复请求结果可复现 | P1 | 一致性 | 固定知识库快照与 mock LLM | 同一 query 与 history 连续请求两次 | 1) 连续两次 POST /rag/chat 2) 比对 rewritten_query 与 sources chunk_id 序列 | 两次 answer_ir.query_rewrite.rewritten_query 相同；两次 sources 的 chunk_id 序列相同；离线模式下 answer 文本相同 | 无 | 存疑 | 依赖 Chroma 召回与 LLM 采样稳定性，测试未固定随机种子或 temperature 断言 |
| TC-RAG-CHAT-058 | 一致性 | 同会话并发 SSE 事件不串流 | P1 | 一致性 | 同一 session_id 与 query | 并发 5 个 POST /rag/chat/stream 请求 | 1) 并发发出 5 个请求 2) 分别收集各自 SSE 流 3) 校验每条流的事件序 | 5 条响应均 HTTP 200；每条流事件序完整且以 done 或 error 收尾；不同流的 delta 文本不交叉进入他人流 | 无 | 存疑 | routes.py:60 pipeline 为模块级单例，search 为同步阻塞调用，无并发保护也无压测证据 |
| TC-RAG-CHAT-059 | 一致性 | 客户端中途断开不写 usage | P1 | 一致性 | 配置有效 key | 流式请求发出后在第 1 个 token 到达时关闭连接 | 1) 发起 POST /rag/chat/stream 2) 收到首个 token 后关闭连接 3) GET /rag/usage 比对 success_requests | 服务端不产生 done 事件；success_requests 与断开前一致；服务端无未捕获异常日志 | 无 | 存疑 | client.py:153 的 record_usage 位于生成器循环之后，连接取消时不执行；未见 GeneratorExit 处理 |
| TC-RAG-CHAT-060 | 流式-存量 | stage 与 meta 先于 LLM 调用发出 | P2 | 正向 | 上游 LLM 人为延迟 2 秒 | {"query":"绝缘电阻测试标准是什么？"} | 1) POST /rag/chat/stream 2) 记录首个事件到达时刻 3) 记录 done 到达时刻 | 首个事件（stage）在 1 秒内到达；done 到达显著晚于首事件；meta.retrieval_ms 为实际检索耗时 | 无 | 已实现 | routes.py:472 与 499 在 llm_stream 调用之前 yield |
| TC-RAG-CHAT-061 | 一致性 | 并发请求下的预算竞态 | P2 | 一致性 | LLM_DAILY_REQUEST_LIMIT_PER_USER 为 1；当天该用户无成功记录 | 并发 2 个 POST /rag/chat 同用户 | 1) 并发发出 2 个请求 2) 读两个状态码 3) GET /rag/usage | 两个请求都可能返回 200（读日志计数与写日志之间无锁），success_requests 最多增加 2；不出现 5xx | 无 | 存疑 | usage_guard.py:91 先读日志再判定，记录在响应后才落盘，存在超发窗口且无测试约束 |
| TC-RAG-CHAT-062 | /health | 健康检查正常字段 | P0 | 正向 | Chroma 可读；未配置 RAG_API_KEY | GET /rag/health | 1) GET /rag/health 2) 校验字段 | HTTP 200；status 等于 "ok"；service 等于 "rag-service"；version 等于 "1.0.0"；chroma_status 等于 "ok"；chroma_count 大于等于 0；embedding_mode 属于 model 或 fallback-hash；llm_provider 非空 | 无 | 已实现 | routes.py:103 直接返回裸字典，无 response_model；rag/tests 无 /rag/health 调用 |
| TC-RAG-CHAT-063 | /health | Chroma 异常时仍报 status ok | P0 | 异常 | CHROMA_PERSIST_DIR 指向不可读路径 | GET /rag/health | 1) 破坏 Chroma 目录 2) GET /rag/health 3) 读 chroma_status 与 status | HTTP 200；status 等于 "ok"；chroma_status 以 "error: " 开头；chroma_count 等于 -1 | 无 | 部分实现 | routes.py:111 把异常降级为字段而非状态码，存活探针会误判服务可用 |
| TC-RAG-CHAT-064 | /usage | 用量摘要字段契约 | P0 | 正向 | 未配置 RAG_API_KEY | GET /rag/usage | 1) GET /rag/usage 2) 校验顶层键与 limits 子键 | HTTP 200；含 date、model、usage_log_path、success_requests、input_chars、output_chars；limits 含 max_input_chars_per_request、daily_request_limit、daily_input_char_limit、daily_request_limit_per_user、hourly_request_limit_per_user | 无 | 已实现 | usage_guard.py:131 usage_summary 结构，无任何测试断言其键集 |
| TC-RAG-CHAT-065 | /usage | 统计只计成功记录 | P0 | 一致性 | 当天日志含 status 为 error 与 blocked 的记录 | 预置 1 条 success、1 条 error、1 条 blocked | 1) GET /rag/usage 2) 比对 success_requests 与 input_chars | success_requests 等于 1；input_chars 只累计 success 记录的 input_chars；被拦截请求不计入上限 | 无 | 部分实现 | usage_guard.py:75 过滤 status 等于 success，导致 blocked 请求可无限重试绕过预算 |
| TC-RAG-CHAT-066 | 权限 | /usage 与 /health 受同一密钥约束 | P1 | 权限 | RAG_API_KEY 为 cap-test-key | 无头与错误头的 GET /rag/health 与 GET /rag/usage | 1) 无鉴权头 GET 两个端点 2) 带正确头重试 | 无头请求两者均 401；带 X-Service-Key 与 X-API-Key 正确值时两者均 200 | test_capability_contract.py::test_auth_accepts_service_key_and_legacy_key（仅 /rag/usage） | 已实现 | /rag/health 的鉴权断言缺失，依赖同一依赖注入但未显式验证 |
| TC-RAG-CHAT-067 | 流式-ep | ep 模式下 verify 结果被丢弃 | P2 | 边界 | 请求头带 X-Service-Key；回答含不受引用支撑的 claim | {"namespace":"ep-docs","question":"外观检测的判定标准是什么？"} | 1) POST /rag/chat/stream 带头 2) 检查得到的全部事件键 3) 检查 done 载荷 | 响应中不出现 answer_ir、warnings、verification 任一字段；verify_answer_ir 的计算结果不对外可见 | 无 | 部分实现 | routes.py:591 计算 answer_ir 后仅用于本地变量，ep done 未携带，属无效计算 |
| TC-RAG-CHAT-068 | /chat 非流式 | 检索异常返回 500 而非空答案 | P0 | 异常 | Chroma 不可用 | {"query":"绝缘电阻测试标准是什么？"} | 1) 破坏 Chroma 2) POST /rag/chat 3) 读状态码 | HTTP 500；body.detail 等于 "Internal server error"；响应中不含伪造答案 | 无 | 已实现 | routes.py:347 与 386 兜底；与流式端点返回 200 加 error 事件的行为不一致 |
| TC-RAG-CHAT-069 | 引用 | chunk_id 缺失时引用 id 回退 | P2 | 边界 | 检索命中 3 条但其中 2 条 chunk_id 为空 | 命中来源 chunk_id 为 "" 的两条 | 1) POST /rag/chat 2) 比对 len(sources) 与 len(answer_ir.citations) | len(answer_ir.citations) 等于 len(sources)；chunk_id 为空的引用其 id 回退为 "source-N" 形式且不为空 | test_answer_ir.py::test_answer_ir_maps_chat_fields_to_structured_contract | 已实现 | models.py:288 回退为 source-{index}；该回退路径无独立断言 |
| TC-RAG-EXT-001 | /extract | 响应字段与 ep ExtractionResult 逐字段对齐 | P0 | 一致性 | rag 服务可启动；`RAG_API_KEY` 已配置；`rag_allowed_dirs` 含暂存目录；测试文件 ≥120 字符 | POST `/rag/extract`，头 `X-Service-Key: cap-route-key`，体 `{"namespace":"ep-docs","targetType":"ASSET","targetId":42,"title":"分选工序规范","scopes":[],"document_id":"ep-docs:ASSET:42","file_path":"/tmp/stage/分选.txt"}` | 1) 启动服务并注入密钥与允许目录 2) 发送请求 3) 解析响应 JSON 键集合与值类型 4) 与 ep `AiCapabilityClient.ExtractionResult` 的 9 个 record 分量逐一比对 | HTTP 200；响应 JSON 顶层键集合恰为 `{name, description, assetTypeCode, tags, summary, categoryCode, scopeHints, evidence, confidence}` 共 9 个；`name/description/assetTypeCode/summary/categoryCode` 为 string；`tags/scopeHints/evidence` 为 array；`confidence` 为 number 且 0.0 ≤ confidence ≤ 1.0；键名保持 camelCase（`assetTypeCode`、`scopeHints`），与 ep record 分量名完全一致 | `rag/tests/test_capability_contract.py::test_extract_route_contract_and_auth`；`::test_extract_engine_returns_ep_contract_fields`；ep 侧 `HttpAiCapabilityClientTest#extractsMetadataFromEndpoint` | 已实现 | `ExtractionResult` 9 字段与 ep `AiCapabilityClient.ExtractionResult(name, description, assetTypeCode, tags, summary, categoryCode, scopeHints, evidence, confidence)` 顺序、名称、类型逐一一致；ep 侧 Jackson 已设 `FAIL_ON_UNKNOWN_PROPERTIES=false`，多余字段不会导致解析失败 |
| TC-RAG-EXT-002 | /extract | 内容不足时按拒答纪律返回空字段而非编造 | P0 | 正向 | 同上；文件正文超短（低于 chunker `MIN_CHUNK_SIZE = 120`） | 文件内容 `"温度异常需先检查夹具。"`（11 字符，切分产出 0 个 chunk） | 1) 发送 `/rag/extract` 2) 断言各字段值 3) 核对 confidence 与 evidence | HTTP 200；`summary == ""`、`description == ""`、`tags == []`、`evidence == []`、`assetTypeCode == ""`、`categoryCode == ""`、`confidence == 0.0`；`name` 仍为传入 title（识别类字段允许保留）；无 5xx、无编造值 | `rag/tests/test_capability_contract.py::test_extract_engine_empty_content_refuses_with_empty_fields`（空白文件分支） | 已实现 | 实测确认：`parts` 为空时 `extract_asset_metadata` 提前 return，`confidence` 保持 0.0；短文档与空白文件走同一早退分支 |
| TC-RAG-EXT-003 | /extract | ep 形态 targetId 派生 document_id 并贯入 evidence | P0 | 正向 | 同上 | `{"namespace":"ep-docs","targetType":"ASSET","targetId":42,"title":"输送模块","scopes":[],"fileContentBase64":"<合法base64>","fileName":"规范.txt"}`（不传 document_id、不传 file_path） | 1) 发送请求 2) 读取 `name` 与 `evidence[0]` | HTTP 200；`name == "输送模块"`；`document_id` 派生为 `ep-docs:ASSET:42` 并作为 evidence 行前缀（形如 `输送模块｜<section_path>｜<片段>`）；`evidence` 条数 ≤3 | `rag/tests/test_capability_contract.py::test_require_ingest_source_base64_stages_into_allowed_dir`（断言 `document_id == "ep-docs:DOCUMENT:9"`） | 已实现 | `_require_ingest_source` 以 `f"{namespace or 'ep'}:{target_type or 'doc'}:{target_id}"` 合成 document_id；`_evidence_lines` 以 title 优先、document_id 兜底作为前缀 |
| TC-RAG-EXT-004 | /extract | fileContentBase64 + fileName 字节运输抽取成功 | P0 | 正向 | 同上；配置 `RAG_STAGING_DIR` 或允许目录可写 | 体含 `fileContentBase64`（正文 ≥120 字符）与 `fileName: "分选规范.txt"` | 1) 发送请求 2) 断言响应与暂存目录清理 | HTTP 200；返回 9 字段契约；`summary`、`tags`、`evidence` 至少一项非空；暂存文件在响应返回后已删除（目录无残留 `*-分选规范.txt`） | `rag/tests/test_capability_contract.py::test_extract_route_accepts_file_content_base64` | 已实现 | `_stage_capability_upload` 落盘到 `RAG_STAGING_DIR` 或 `rag_allowed_dirs[0]/capability-staging/<namespace>`，路由 `finally` 中 `os.remove` |
| TC-RAG-EXT-005 | /extract | scopeHints 取自 scopes 各对象值并保序去重 | P0 | 一致性 | 同上 | `scopes: [{"productLine":"L1","base":"B2","empty":""},{"base":"B2"}]` | 1) 发送请求 2) 断言 `scopeHints` 集合与顺序 | HTTP 200；`scopeHints` 为 array；`set(scopeHints) == {"L1","B2"}`；空值不进入；重复值只保留首次出现顺序；元素数 ≤12 | `rag/tests/test_capability_contract.py::test_extract_engine_returns_ep_contract_fields`（`set(payload["scopeHints"]) == {"L1","B2"}`） | 已实现 | `_scope_hints` 遍历 dict 值、去空、去重后 `[:12]` |
| TC-RAG-EXT-006 | /extract | camelCase 与 snake_case 入参别名双向兼容 | P1 | 一致性 | 同上 | 两组等价请求：A 组 `{"targetType":"ASSET","targetId":7,"fileContentBase64":"...","fileName":"a.txt"}`；B 组 `{"target_type":"ASSET","target_id":7,"file_content_base64":"...","file_name":"a.txt"}` | 1) 分别发送 A/B 两组请求 2) 比对两次响应 | 两次均 HTTP 200；两次响应 JSON 完全一致（同一 document_id 派生、同一 name） | `rag/tests/test_capability_contract.py::test_chat_request_accepts_ep_shape_aliases`（ChatRequest）；`:125` 起 extract 测试仅覆盖 camelCase | 已实现 | `AliasChoices` 已声明 `file_content_base64/fileContentBase64`、`file_name/fileName`、`target_type/targetType`、`target_id/targetId` |
| TC-RAG-EXT-007 | /extract | 既无 file_path 也无 fileContentBase64 时返回 400 | P1 | 异常 | 同上 | 体 `{"namespace":"ep-docs","targetType":"DOCUMENT","targetId":43,"title":"x","scopes":[]}` | 1) 发送请求 2) 读取 status 与 detail | HTTP 400；`detail` 为字符串且包含 `file_path` 与 `fileContentBase64` 两个关键词；不返回 500、不返回 422 | `rag/tests/test_capability_contract.py::test_extract_route_contract_and_auth`（尾部 `missing_file` 断言） | 已实现 | `_require_ingest_source` 抛 `HTTPException(400, "缺少可解析文件：请提供 file_path，或 ep 契约的 fileContentBase64 + fileName")`，路由先 `except HTTPException: raise` |
| TC-RAG-EXT-008 | /extract | file_path 越权读取被拒绝 | P0 | 异常 | `rag_allowed_dirs` 仅含 `/tmp/stage` | 体 `{"document_id":"a","file_path":"/etc/passwd"}` | 1) 发送请求 2) 读取 status 与 detail | HTTP 400；`detail` 以 `Path outside allowed directories:` 开头；响应体不含文件内容；不得返回 200 | 无（`_validate_file_path` 无独立单测，仅经 extract/ingest 间接覆盖） | 已实现 | 实测返回 `400 {"detail":"Path outside allowed directories: /etc/passwd"}`；`_validate_file_path` 先 `resolve()` 再做 `is_relative_to` 前缀校验，防目录穿越 |
| TC-RAG-EXT-009 | /extract | 非法 Base64 内容返回 400 | P1 | 异常 | 同上 | `{"namespace":"ep-docs","targetId":10,"scopes":[],"fileContentBase64":"!!!not-base64!!!","fileName":"a.txt"}` | 1) 发送请求 2) 读取 status 与 detail | HTTP 400；`detail == "fileContentBase64 不是合法的 Base64 内容"` | `rag/tests/test_capability_contract.py::test_require_ingest_source_base64_validation`（引擎层 `pytest.raises(ValueError)`，非 HTTP 层） | 已实现 | `base64.b64decode(..., validate=True)` 失败抛 `ValueError`，路由 `except ValueError` 映射 400 |
| TC-RAG-EXT-010 | /extract | Base64 解码后为空字节流返回 400 | P2 | 边界 | 同上 | `{"namespace":"ep-docs","targetId":11,"scopes":[],"fileContentBase64":"","fileName":"a.txt"}`（空串）与 `fileContentBase64:"=="`（解码为空） | 1) 发送两次请求 2) 读取 status 与 detail | 两次均 HTTP 400；空串形态 `detail` 含 `file_path`（视为未提供运输字段）；解码为空形态 `detail == "fileContentBase64 为空"` | 无 | 已实现 | `_stage_capability_upload` 显式判 `if not raw: raise HTTPException(400, "fileContentBase64 为空")` |
| TC-RAG-EXT-011 | /extract | Base64 解码后超过 50MB 上限返回 400 | P2 | 边界 | 同上；可用稀疏/重复字节构造 | `fileContentBase64` 解码后 50MB + 1 字节，`fileName:"big.pdf"` | 1) 发送请求 2) 检查 status 与暂存目录 | HTTP 400；`detail == "fileContentBase64 超过 50MB 大小上限"`；暂存目录无落盘文件（超限检查在写盘前） | 无 | 已实现 | 常量 `_CAPABILITY_UPLOAD_MAX_BYTES = 50 * 1024 * 1024`，长度校验先于 `staged.write_bytes` |
| TC-RAG-EXT-012 | /extract | fileName 缺失扩展名时返回 400 | P2 | 边界 | 同上 | `{"namespace":"ep-docs","targetId":12,"scopes":[],"fileContentBase64":"eA==","fileName":"规范"}`；以及完全省略 fileName | 1) 分别发送两次请求 2) 读取 status 与 detail | 两次均 HTTP 400；省略 fileName 时 `detail` 含 `缺少 fileName`；无扩展名时 `detail` 含 `fileName 缺少扩展名` | `rag/tests/test_capability_contract.py::test_require_ingest_source_base64_validation`（no_name / no_ext 两个分支） | 已实现 | 分支由 `Path(request.file_name or "").name.strip()` 与 `Path(file_name).suffix` 判定；`Path(name).name` 同时剥离路径，防目录注入 |
| TC-RAG-EXT-013 | /extract | 服务密钥鉴权：双头兼容、缺失或错误 401 | P0 | 权限 | `rag_api_key` 已配置为 `cap-route-key` | 三次请求：头 `X-Service-Key: cap-route-key`、头 `X-API-Key: cap-route-key`、无密钥头；一次请求头 `X-Service-Key: nope` | 1) 逐一发送 2) 记录 status | 前两次 HTTP 200；无密钥与错误密钥均 HTTP 401 且 `detail == "Invalid or missing API key"`；密钥未配置时（`rag_api_key==""`）全部放行返回 200 | `rag/tests/test_capability_contract.py::test_extract_route_contract_and_auth`；`::test_auth_accepts_service_key_and_legacy_key`；`::test_auth_skipped_when_key_unconfigured` | 已实现 | `verify_api_key` 取 `X-Service-Key` 优先、`X-API-Key` 兜底；两侧均为常量空时跳过校验并记录启动告警 |
| TC-RAG-EXT-014 | /extract | 空请求体不报 422 而按缺文件返回 400 | P1 | 边界 | 同上 | 体 `{}` | 1) 发送请求 2) 读取 status 与 detail | HTTP 400；`detail` 含 `缺少可解析文件`；不得返回 422（`IngestRequest` 全部字段可选） | 无 | 已实现 | 实测返回 `400 {"detail":"缺少可解析文件：请提供 file_path，或 ep 契约的 fileContentBase64 + fileName"}` |
| TC-RAG-EXT-015 | /extract | LLM 预算超限时契约要求 429，实际被内部降级吞没 | P0 | 异常 | `_llm_live()` 为真；LLM 调用抛 `LlmBudgetExceeded(limit_type=request_input_chars)` | 正文 ≥120 字符的正常文档 | 1) 令 LLM 调用抛 `LlmBudgetExceeded` 2) 发送 `/rag/extract` 3) 记录 status 与 body | 期望 HTTP 429 且 `detail.code == "LLM_BUDGET_EXCEEDED"`、含 `limit_type/limit/used/user_id`；**实测 HTTP 200** 且走确定性降级（`summary` 非空、`confidence == 0.6`、`assetTypeCode == ""`），429 分支实际不可达 | 无（`rag/tests` 无任何 `LLM_BUDGET_EXCEEDED`/429 断言） | 部分实现 | `extract_asset_metadata` 内 `except Exception` 环绕 `_extract_via_llm`，`LlmBudgetExceeded` 继承 `Exception` 故被吞掉；路由的 `except LlmBudgetExceeded -> 429` 对该路径为死代码 |
| TC-RAG-EXT-016 | /extract | LLM 不可用或输出非 JSON 时降级且不返回 5xx | P1 | 异常 | `_llm_live()` 为假，或 LLM 返回不可解析文本 | 正文 ≥120 字符的正常文档 | 1) 分别以 mock 关闭 LLM、以 mock 返回非 JSON 内容 2) 发送请求 | 两次均 HTTP 200；字段契约完整；降级路径下 `assetTypeCode == ""`、`categoryCode == ""`、`0.0 <= confidence <= 1.0`；`summary`/`tags` 至少一项非空 | `rag/tests/test_capability_contract.py::test_extract_engine_returns_ep_contract_fields`（`_llm_live` 置假）；`::test_extract_engine_empty_content_refuses_with_empty_fields` | 已实现 | `_parse_json_object` 支持裸 JSON、```json 围栏与首尾截取，均失败则抛错由上层 `except Exception` 降级到 `_fill_deterministic` |
| TC-RAG-EXT-017 | /extract | 短文档（低于切分阈值）返回空字段 | P1 | 边界 | 同上 | 文件正文 120 字符以内但非空白，例如 `"分选工序：外观检测、OCV 测试与内阻判定。"` | 1) 发送请求 2) 断言字段 | HTTP 200；`summary == ""`、`tags == []`、`evidence == []`、`confidence == 0.0`；`name` 保留 title；不得因内容短而报错 | 无（现有空内容用例用的是纯空白文件，未覆盖“有内容但低于 120 字符”分支） | 已实现 | 实测：`chunker.MIN_CHUNK_SIZE = 120`，短于阈值时 `chunk_document` 返回 0 个 chunk，`parts` 为空触发早退 |
| TC-RAG-EXT-018 | /extract | 抽取字段数量与长度上限受控 | P2 | 边界 | LLM 路径与确定性路径各跑一次 | 长文档（>6000 字符正文）+ LLM 返回 20 条 tags、20 条 evidence、10 条 scopeHints | 1) 发送请求 2) 统计 `tags`/`evidence`/`scopeHints` 条数与各字符串长度 | HTTP 200；确定性路径 `tags` ≤8、`evidence` ≤3、`summary` ≤200、单条 evidence 片段 ≤90 字符；LLM 路径 `tags` ≤8、单字符串 ≤200；`confidence` 被 clamp 到 [0.0, 1.0] | `rag/tests/test_capability_contract.py::test_extract_engine_returns_ep_contract_fields`（仅断言类型，未断言上限） | 部分实现 | `_MAX_TAGS=8`、`_MAX_EVIDENCE=3`、`_EVIDENCE_SNIPPET_CHARS=90` 仅在确定性路径生效；`_coerce_extraction_result` 中 `evidence`/`scopeHints` 未切片，LLM 路径条数无上限，可能返回超长数组 |
| TC-RAG-EXT-019 | /artifacts/image/contract | allowed 仅在“显式触发 + 有可追溯来源”时为真 | P0 | 正向 | 服务可启动；`IMAGE_GEN_ENABLED` 默认 false | 四次组合：`requested_by_user` 真/假 × `sources` 非空/空 | 1) 逐一发送 2) 记录 `allowed` | 仅“真 + 非空来源”返回 `allowed == true`；其余三种返回 `allowed == false`；`allowed` 语义为契约是否满足证据与授权，与本机外发开关无关；`renderer == "image-contract"`、`status == "contract_only"`、`async_required == true` | `rag/tests/test_quality_eval_suite.py::test_quality_eval_image_artifact_contract_redacts_and_inherits_sources`；`::test_quality_eval_image_artifact_contract_requires_explicit_user_request`（均为引擎层函数调用） | 已实现 | `allowed = requested_by_user and bool(source_ids)`；api 侧调用恒传 `requestedByUser=true`，故线上 `allowed` 实际只由 `source_ids` 是否为空决定 |
| TC-RAG-EXT-020 | /artifacts/image/contract | 脱敏正例：邮箱与密钥被替换且计数写入 redaction_report | P0 | 正向 | 同上 | `question:"请生成EOL测试示意图，联系 test@example.com"`、`answer:"... token-abcdefghijklmnop 不能进入图片提示词。"`、`sources:[{"chunk_id":"chunk-image","document_id":"d1","snippet":"EOL测试前确认夹具和线缆。"}]`、`requested_by_user:true` | 1) 发送请求 2) 检查 `sanitized_prompt` 与 `redaction_report` | HTTP 200；`sanitized_prompt` 不含 `test@example.com`、不含 `token-abcdefghijklmnop`；含 `[已脱敏:email]` 与 `[已脱敏:api_key]`；`redaction_report == {"email": 1, "api_key": 1}` | `rag/tests/test_quality_eval_suite.py::test_quality_eval_image_artifact_contract_redacts_and_inherits_sources` | 已实现 | `_SENSITIVE_PATTERNS` 顺序为 email → phone → api_key → long_id；`api_key` 正则 `\b(?:sk\ |
| TC-RAG-EXT-021 | /artifacts/image/contract | 脱敏反例：正常中文与产线参数不被误脱敏 | P0 | 正向 | 同上 | `question:"EOL测试包括哪些项目？"`、`answer:"EOL测试包括绝缘电阻、耐压和DCR测试，夹具确认后记录结果。电压窗口 1000V，内阻不低于500MΩ。"`、`sources:[{"chunk_id":"chunk-image","snippet":"EOL测试包括绝缘电阻、耐压和DCR测试。"}]` | 1) 发送请求 2) 断言 `redaction_report` 为空对象、`sanitized_prompt` 保留原文关键术语 | HTTP 200；`redaction_report == {}`；`sanitized_prompt` 完整含 `绝缘电阻`、`耐压`、`DCR`、`1000V`、`500MΩ`，无任何 `[已脱敏:` 标记 | 无（现有脱敏用例只有正例，无反例断言） | 已实现 | 实测 `_redact` 对该组输入返回 `({}, ...)`：`1000V`/`500MΩ` 均未触发 phone（不足 9 位连续数字）与 long_id（<24 位） |
| TC-RAG-EXT-022 | /artifacts/image/contract | 12 位纯数字设备序列号被误判为 phone 而脱敏 | P1 | 边界 | 同上 | `answer:"设备序列号 123456789012 已登记。"` | 1) 发送请求 2) 断言 `sanitized_prompt` 与 `redaction_report` | 期望 `redaction_report == {}` 且序列号原文保留；实测 `redaction_report == {"phone": 1}` 且原文被替换为 `[已脱敏:phone]`，序号信息在图片提示词中丢失 | 无 | 部分实现 | phone 正则 `(?<!\d)(?:\+?\d[\d -]{7,}\d)(?!\d)` 未限定国际/国内号码格式，任何 9 位以上连续数字（含工单号、序列号、长数值参数）都被判为手机号 |
| TC-RAG-EXT-023 | /artifacts/image/contract | 24 位以上 chunk_id/UUID 被 long_id 规则吞掉 | P1 | 边界 | 同上；sources 的 `snippet` 中含长标识 | `sources:[{"chunk_id":"chunk-abcdefghijklmnopqrstuvwx","document_id":"48e08959-e1b4-4b58-9695-74d2f3779257","snippet":"依据 chunk-abcdefghijklmnopqrstuvwx 定位原文。"}]` | 1) 发送请求 2) 对比 `sanitized_prompt`、`inherited_source_ids`、`inherited_document_ids` | `redaction_report["long_id"] == 1`，`sanitized_prompt` 中该标识被替换为 `[已脱敏:long_id]`；而 `inherited_source_ids`、`inherited_document_ids` 仍为原值（这两列不经 `_redact`）。期望长标识只在“疑似密钥”时脱敏，纯 chunk id/UUID 不应被当作敏感信息 | 无 | 部分实现 | `long_id` 正则 `\b[A-Za-z0-9_-]{24,}\b` 无前缀约束；本项目 document_id 本身即 UUID（36 字符），一旦出现在 snippet/answer 中即被静默替换 |
| TC-RAG-EXT-024 | /artifacts/image/contract | 来源与文档 id 继承保序去重 | P1 | 一致性 | 同上 | `sources` 含 3 条：两条同 `chunk_id`（其中一条 `id` 字段优先）、两条同 `document_id`、一条无 `document_id` | 1) 发送请求 2) 检查继承数组 | HTTP 200；`inherited_source_ids` 为 array 且按 `id` > `chunk_id` > `source-<index>` 优先级取值、保序去重；`inherited_document_ids` 只含非空 `document_id` 且去重；缺 `document_id` 的来源不产生空串元素 | `rag/tests/test_quality_eval_suite.py::test_quality_eval_image_artifact_contract_redacts_and_inherits_sources`（仅断言 `inherited_source_ids == ["chunk-image"]`） | 已实现 | `_source_id` 与 `_document_id` 按序取值；`if document_id and document_id not in document_ids` 过滤空串 |
| TC-RAG-EXT-025 | /artifacts/image/contract | 无可追溯来源时返回 image_requires_sources 警告 | P1 | 异常 | 同上 | `sources: []`、`requested_by_user: true` | 1) 发送请求 2) 检查 `allowed` 与 `safety_warnings` | HTTP 200；`allowed == false`；`safety_warnings` 中存在 `code == "image_requires_sources"` 且 `severity == "warning"` 的元素；`inherited_source_ids == []` | 无（现有用例只覆盖 explicit_request 与 disabled 两种警告） | 已实现 | 三个警告分支彼此独立追加：`image_requires_explicit_request`(info)、`image_requires_sources`(warning)、`image_generation_disabled`(info) |
| TC-RAG-EXT-026 | /artifacts/image/contract | 非用户显式触发时给出 image_requires_explicit_request | P1 | 正向 | 同上 | `requested_by_user: false`、来源非空 | 1) 发送请求 2) 检查 `safety_warnings[0]` | HTTP 200；`allowed == false`；`safety_warnings[0].code == "image_requires_explicit_request"` 且 `severity == "info"` | `rag/tests/test_quality_eval_suite.py::test_quality_eval_image_artifact_contract_requires_explicit_user_request` | 已实现 | 该警告为 `warnings` 列表首个元素，顺序在另外两条之前 |
| TC-RAG-EXT-027 | /artifacts/image/contract | 图片生成外发开关关闭时的降级契约 | P2 | 一致性 | `IMAGE_GEN_ENABLED` 未开启（默认 false） | 与 TC-RAG-EXT-020 相同的合法请求 | 1) 发送请求 2) 读取 `safety_warnings` 与 `metadata` | HTTP 200；`safety_warnings` 含 `code == "image_generation_disabled"`（severity `info`）；`metadata.image_gen_enabled == false`、`metadata.provider == "not_configured"`、`metadata.requires_async_worker == true`、`metadata.default_auto_generate == false`；`allowed` 仍可为 true（仅返回脱敏提示词契约，不外发） | 无 | 已实现 | `config.image_gen_enabled` 由 `IMAGE_GEN_ENABLED` 读取；关闭时只追加 info 警告，不改变 `allowed` |
| TC-RAG-EXT-028 | /artifacts/image/contract | 缺少必填 answer 字段返回 422 | P2 | 异常 | 同上 | 体 `{"question":"q"}`（省略 `answer`） | 1) 发送请求 2) 读取 status 与 detail | HTTP 422；`detail[0].loc == ["body","answer"]`、`detail[0].type == "missing"`；不得返回 200 或 500 | 无 | 已实现 | 实测返回 `422 {"detail":[{"type":"missing","loc":["body","answer"],"msg":"Field required",...}]}`；`ImageArtifactContractRequest.answer` 无默认值 |
| TC-RAG-EXT-029 | /artifacts/image/contract | 提示词与来源片段按上限截断 | P2 | 边界 | 同上 | `answer` 2000 字符；`sources` 6 条、每条 snippet 500 字符 | 1) 发送请求 2) 统计 `sanitized_prompt` 长度与片段条数 | HTTP 200；`sanitized_prompt` 长度 ≤1600；`回答摘要：` 行后内容 ≤900 字符；`引用依据：` 下最多 4 条 `- ` 片段、每条 ≤220 字符；`inherited_source_ids` 长度仍为 6（继承不受截断影响） | 无 | 已实现 | `_redact` 之后 `sanitized_prompt[:1600]`；raw_prompt 中 `answer.strip()[:900]`、`snippet[:220]`、`snippets[:4]` |
| TC-RAG-EXT-030 | /artifacts/image/contract | 未携带服务密钥时 401 | P1 | 权限 | `rag_api_key` 已配置 | 无密钥头 / 错误密钥头 / `X-API-Key` 正确密钥 | 1) 逐一发送 2) 记录 status | 无密钥与错误密钥 HTTP 401；`X-API-Key` 正确密钥 HTTP 200（与 `X-Service-Key` 等价） | 无（`test_auth_accepts_service_key_and_legacy_key` 只覆盖 `/rag/usage`） | 已实现 | 该端点同样挂载 `dependencies=[Depends(verify_api_key)]`，鉴权语义与全路由一致 |
| TC-RAG-EXT-031 | /diagram/generate | mindmap 生成 DiagramIR 基本契约 | P0 | 正向 | 服务可启动；LLM 可用或允许降级 | 体 `{"title":"工艺风险整理","content":"<含重复领域术语的工序正文>","type":"mindmap","source_ids":["source-a"],"max_steps":8}` | 1) 发送请求 2) 校验顶层字段与节点结构 | HTTP 200；`schema_version == "diagram-ir/v2"`、`type == "mindmap"`、`renderer == "diagram-ir"`；`nodes` 每个元素含 `id/label/kind/description/source_ids/metadata`；`edges` 每个元素含 `source/target/relation/label/metadata`；节点 `kind` 均属合法取值集合；`max_steps` 生效约束节点规模 | `rag/tests/test_diagram_ir.py::test_build_keyword_diagram_ir_groups_mindmap_keywords`；`::test_build_llm_diagram_ir_uses_structured_business_nodes_only`（引擎层） | 已实现 | 路由 `diagram_type` 归一后传 `build_llm_diagram_ir`；`response_model=DiagramIR` 保证键名 snake_case |
| TC-RAG-EXT-032 | /diagram/generate | flowchart 泳道（lanes）契约满足 api 校验 | P0 | 正向 | LLM 可用且返回多角色流程 | 体 `{"title":"多角色处置流程","content":"<含操作员/工程师/质检多角色步骤的正文>","type":"flowchart","source_ids":["s1","s2"],"max_steps":10}` | 1) 发送请求 2) 校验 `lanes` 与 `nodes[].metadata.lane_id` 关系 | HTTP 200；`type == "flowchart"`；`lanes` 为 array 且每个 `id` 非空且唯一（与 api `diagramIRSchema` 的 superRefine 一致）；属于流程图节点种类的每个 `nodes[i].metadata.lane_id` 均为非空字符串且指向存在的 lane；`nodes[i].source_ids`、`nodes[i].metadata` 字段必存在（api 侧为 required） | `rag/tests/test_diagram_ir.py::test_build_llm_diagram_ir_outputs_swimlane_metadata_for_multi_role_flow`；消费端约束见 `api/src/services/rag/schemas.ts` `diagramIRSchema` | 已实现 | `_structured_output_to_diagram_ir` 输出 `lanes` 与 `metadata.lanes`；api 侧 `FLOWCHART_NODE_KINDS` 校验 `metadata.lane_id` 必填 |
| TC-RAG-EXT-033 | /diagram/generate | type 非 mindmap/flowchart 时返回 400 | P2 | 异常 | 同上 | 体 `{"content":"x","type":"pie"}`；再测 `type":"MindMap"`（带大小写） | 1) 两次发送 2) 读取 status 与 detail | 非法值 HTTP 400 且 `detail == "type must be mindmap or flowchart"`；`"MindMap"` 因路由先做 `.strip().lower()` 而归一化为合法值返回 200 | 无 | 已实现 | 实测 `{"content":"x","type":"pie"} → 400 {"detail":"type must be mindmap or flowchart"}`；`request.type.strip().lower()` 在集合判定前执行 |
| TC-RAG-EXT-034 | /diagram/generate | content 缺失返回 422、全空白返回 400 | P1 | 异常 | 同上 | 体 `{"type":"mindmap"}`（缺 content）；体 `{"content":"   ","type":"mindmap"}`（全空白） | 1) 两次发送 2) 读取 status 与 detail | 缺字段 HTTP 422 且 `detail[0].loc == ["body","content"]`、`type == "missing"`；全空白 HTTP 400 且 `detail == "content is required"` | 无 | 已实现 | 实测确认两条分支；`DiagramGenerateRequest.content` 为必填，路由再对空白值显式 400 |
| TC-RAG-EXT-035 | /diagram/generate | max_steps 越界被模型校验拒绝 | P1 | 边界 | 同上 | `max_steps` 取 1、2、12、13 四个值 | 1) 逐一发送 2) 记录 status | `max_steps` 为 1 或 13 时 HTTP 422 且 `detail[0].loc == ["body","max_steps"]`（`ge:2`/`le:12`）；为 2 与 12 时 HTTP 200 并正常返回 | 无 | 已实现 | 实测 `max_steps:99 → 422 less_than_equal le:12`；模型声明 `Field(default=8, ge=2, le=12)`；api 调用方固定传 10 |
| TC-RAG-EXT-036 | /diagram/generate | title 为空或空白时回退默认标题 | P2 | 边界 | 同上 | `title` 分别取 `""`、`"   "`、省略该字段 | 1) 逐一发送 2) 读取 `title` | 三种情况均 HTTP 200 且 `title == "AI 整理"`；显式传 `"工艺风险整理"` 时原样返回 | 无 | 已实现 | 路由 `title=request.title.strip() or "AI 整理"`；模型默认值同为 `"AI 整理"` |
| TC-RAG-EXT-037 | /diagram/generate | LLM 结构化输出非法时降级关键词生成 | P0 | 异常 | LLM 可达但返回非 JSON / 结构不合法 | mock LLM 返回 `{"model":"fake-bad","content":"无法生成 JSON"}`；体 `{"title":"工艺风险整理","content":"回答正文：温度窗口需要保持稳定，压力控制异常会触发安全风险。","type":"mindmap","source_ids":["source-a"]}` | 1) 发送请求 2) 读取 `metadata` 与 `notes` | HTTP 200；`metadata.generation_mode == "keyword_fallback"`；`metadata.fallback_reason` 为截断到 240 字符的原因串；`notes` 末条含“使用关键词规则降级生成”；`type` 保持请求值；不返回 5xx | `rag/tests/test_diagram_ir.py::test_build_llm_diagram_ir_falls_back_when_structured_output_is_invalid` | 已实现 | `except (ValueError, json.JSONDecodeError, ValidationError, TypeError)` 捕获后调用 `build_keyword_diagram_ir` 并补齐 `generation_mode`/`fallback_reason` |
| TC-RAG-EXT-038 | /diagram/generate | LLM 预算超限返回 429 与结构化错误体 | P0 | 异常 | LLM 调用抛 `LlmBudgetExceeded` | 体 `{"content":"<200 字符正文>","type":"mindmap"}` | 1) 令 LLM 调用抛预算异常 2) 发送请求 3) 读取 status 与 detail | HTTP 429；`detail.code == "LLM_BUDGET_EXCEEDED"`；`detail` 含 `message/limit_type/limit/used/user_id` 五个键；`limit_type` 为 `request_input_chars`/`global_daily_requests`/`user_daily_requests`/`user_hourly_requests`/`global_daily_input_chars` 之一 | 无（`rag/tests` 无 429 断言） | 已实现 | 实测返回 `429 {"detail":{"code":"LLM_BUDGET_EXCEEDED","message":"单次输入过大","limit_type":"request_input_chars","limit":10,"used":99,"user_id":"service"}}` |
| TC-RAG-EXT-039 | /diagram/generate | 内部异常时 500 响应泄露原始异常文本 | P1 | 异常 | LLM 抛出非 HTTPException 的运行时异常 | mock 抛 `RuntimeError("chroma 内部路径 /data/secret/chroma.sqlite3 连接失败")` | 1) 发送请求 2) 读取 status 与 detail | HTTP 500；实测 `detail == "chroma 内部路径 /data/secret/chroma.sqlite3 连接失败"`，即原始异常字符串直接外泄。期望 `detail` 为固定文案（如 `Internal server error`），与同一文件 `/documents`、`/documents/ingest` 的处理方式一致 | 无 | 部分实现 | 该路由 `except Exception as e: raise HTTPException(500, detail=str(e))`；同文件 `/documents` 与 `/documents/ingest` 用的是固定 `"Internal server error"`，此处不一致 |
| TC-RAG-EXT-040 | /diagram/generate | 响应字段名与 api diagramIRSchema 对齐 | P0 | 一致性 | 同上 | 一次成功响应 | 1) 发送请求 2) 按 `api/src/services/rag/schemas.ts` 的 `diagramIRSchema` 逐字段比对 | HTTP 200；顶层必含 `title/objective/type/layout_hint/nodes/edges/notes/metadata`；`diagram_type` 为可选别名且与 `type` 同值；`nodes[].source_ids`、`nodes[].metadata`、`edges[].metadata` 必须存在（api 侧非 optional）；`validation` 可空但存在时必须含 `can_generate/quality_score/warnings/errors`；全部字段为 snake_case，无 camelCase 泄漏 | 无（HTTP 层无用例；消费端 schema 在 `api/src/services/rag/schemas.ts`） | 已实现 | `DiagramIR` 缺省序列化为 snake_case，与 api `diagramIRSchema` 的字段名一致；`diagram_type` 由 `@property` 提供同值别名 |
| TC-RAG-EXT-041 | /diagram/generate | 节点种类、边关系受约束且来源覆盖可追溯 | P1 | 一致性 | 同上 | mock LLM 返回含非法 `kind` 的节点与自环边 | 1) 发送请求 2) 读取 `validation` 与 `quality_warnings` | HTTP 200；`validation.warnings`/`errors` 中给出非法节点种类与非法边关系的告警（含 `node_ids`/`edge_ids`）；自环边与悬空边被剔除；`validation.required_source_ids`、`covered_source_ids`、`missing_source_ids`、`citation_coverage_ratio` 与 `source_ids` 输入一致；`quality_score` ∈ [0.0, 1.0] | `rag/tests/test_diagram_ir.py::test_validate_diagram_ir_rejects_invalid_edges_and_tracks_source_coverage`；`::test_validate_diagram_ir_enforces_flowchart_quality_gates` | 已实现 | `diagram_validation.py` 校验 `ALLOWED_NODE_KINDS`/`ALLOWED_EDGE_RELATIONS` 并回填来源覆盖三数组 |
| TC-RAG-EXT-042 | /diagram/generate | 未携带服务密钥时 401 | P1 | 权限 | `rag_api_key` 已配置且正确值已知 | 分别以无密钥头、错误密钥、正确 `X-Service-Key` 请求 | 1) 逐一发送 2) 记录 status | 无密钥与错误密钥 HTTP 401；正确密钥 HTTP 200；401 响应体不包含能力服务内部信息 | 无 | 已实现 | 该端点亦挂载 `Depends(verify_api_key)` |
| TC-RAG-EXT-043 | /knowledge/graph/plan | 返回轻量知识图谱三件套 | P0 | 正向 | 服务可启动 | 体 `{"content":"温度异常需要先检查夹具再复核压力，异常须隔离处置。","sources":[{"document_id":"d1","chunk_id":"c1","snippet":"s"}]}` | 1) 发送请求 2) 校验顶层结构 | HTTP 200；`schema_version == "knowledge-graph/v1"`；`nodes`、`edges` 均为 array；`stats` 含 `node_count`、`edge_count`、`source_count` 三个整数；`stats.node_count == len(nodes)`、`stats.edge_count == len(edges)` | 无（`rag/tests`、`rag/evals` 均无 `build_lightweight_knowledge_graph` 任何断言） | 已实现 | 实测返回 `{"schema_version":"knowledge-graph/v1","nodes":[...],"edges":[...],"stats":{...}}`；端点为 `response_model` 为空的裸 dict |
| TC-RAG-EXT-044 | /knowledge/graph/plan | content 缺失 422、全空白 400 | P1 | 异常 | 同上 | 体 `{}`（缺 content）；体 `{"content":"  "}` | 1) 两次发送 2) 读取 status 与 detail | 缺字段 HTTP 422（`loc == ["body","content"]`）；全空白 HTTP 400 且 `detail == "content is required"` | 无 | 已实现 | 实测 `{"content":"  "} → 400 {"detail":"content is required"}`；`KnowledgeGraphPlanRequest.content` 无默认值 |
| TC-RAG-EXT-045 | /knowledge/graph/plan | 节点边按权重降序且 id 规范可解析 | P1 | 一致性 | 同上 | 含重复关键词与参数表达式的正文 | 1) 发送请求 2) 检验排序与 id 形态 | HTTP 200；`nodes` 按 `weight` 严格非递增、`edges` 按 `weight` 严格非递增；每个 `node.id` 形如 `"<type>:<label 归一化前 60 字符>"`（label 转小写、空白转 `-`）；`edge.id` 形如 `"<source>-><relation>-><target>"`；无自环边（`source != target`）；`edge.type` 为该 edge 的 relation 值 | 无 | 已实现 | `sorted(..., key=lambda item: item["weight"], reverse=True)`；`_add_edge` 显式丢弃 `source == target` |
| TC-RAG-EXT-046 | /knowledge/graph/plan | 风险、处置、流程与参数节点分类识别 | P1 | 正向 | 同上 | 正文含 `风险`/`异常`、`处理`/`排查`、`先`/`然后`、以及 `电压为1000V` 形式的参数表达式 | 1) 发送请求 2) 统计各类节点 | HTTP 200；出现 `type == "risk"`、`type == "handling"`、`type == "process"` 的节点；参数表达式额外生成 `type == "parameter"` 节点且其 `metadata.value` 为匹配到的数值串（如 `"1000V"`），并从根节点引出 `has_parameter` 边；来源非空时生成 `type == "document"` 节点与 `cites` 边 | 无 | 已实现 | `RISK_RE`/`HANDLING_RE`/`PROCESS_RE`/`PARAMETER_RE` 四组正则分类；`_add_node` 将 `value` 写入 metadata |
| TC-RAG-EXT-047 | /knowledge/graph/plan | sources 截断为 12 条但 stats.source_count 计全量 | P2 | 一致性 | 同上 | `sources` 传入 20 条各不相同的来源对象 | 1) 发送请求 2) 对比 document 节点数与 `stats.source_count` | HTTP 200；`type == "document"` 的节点数为 12（超出的 8 条被丢弃）；但 `stats.source_count == 20`。期望统计与实际建图一致，或 `stats` 中显式区分 `source_count` 与 `truncated_source_count` | 无 | 部分实现 | `build_lightweight_knowledge_graph` 用 `sources[:12]` 建节点，而 `stats.source_count = len(sources)` 取全量，二者口径不一致，消费方按 stats 判断覆盖度会偏乐观 |
| TC-RAG-EXT-048 | /knowledge/graph/plan | 端点接线状态与消费方契约不明 | P2 | 一致性 | 全仓可检索 | 在 ai-rag 的 `api/`、`web/` 与 ep 的 `backend/`、`frontend/` 中检索 `/rag/knowledge/graph/plan` | 1) 全仓 grep 该路径 2) 检查是否有 HTTP 层用例或 zod/Java 契约校验 | 检索结果：无任何调用方与消费方契约。api 侧 `/knowledge/graph` 走本地 `buildKnowledgeGraph()`，并未调用 rag 端点。期望明确该端点是「保留供未来接入（需补契约校验用例）」还是「死代码（可移除）」，避免无契约保护的裸 dict 长期暴露 | 无 | 存疑 | 端点实现完整且可正常返回，但零调用方、零契约校验、零自动化用例；是否为有意保留无法从代码判定 |
| TC-RAG-EXT-049 | /knowledge/graph/plan | 未携带服务密钥时 401 | P1 | 权限 | `rag_api_key` 已配置 | 无密钥头 / 错误密钥 / 正确密钥 | 1) 逐一发送 2) 记录 status | 无密钥与错误密钥 HTTP 401；正确密钥 HTTP 200 | 无 | 已实现 | 该端点同样挂载 `Depends(verify_api_key)` |
| TC-RAG-EXT-050 | /knowledge-gaps/cluster-drafts | 高频失败问题聚类生成五类待审草稿 | P0 | 正向 | 服务可启动 | 体含 3 条 `failed_questions`（fq-1 refusal/confidence 0.1、fq-2 low_confidence/0.35 同术语、fq-3 另一术语），`min_frequency: 2` | 1) 发送请求 2) 校验结果结构 | HTTP 200；`schema_version == "knowledge-gap-cluster/v1"`；`clusters` 长度 1；该簇 `frequency_count == 2`、`sample_failed_question_ids == ["fq-1","fq-2"]`、`gap_type == "mixed"`、`event_types == ["low_confidence","refusal"]`；每簇同时给出 `term_candidates`、`alias_candidates`、`faq_drafts`、`knowledge_card_drafts`、`document_supplement_suggestions` 五类非空草稿；`ignored_count == 1` | `rag/tests/test_knowledge_gap_drafts.py::test_build_knowledge_gap_cluster_drafts_generates_review_candidates` | 已实现 | `_build_cluster` 一次性产出五类候选；`_severity` 由频次 + 事件权重 + 低置信计数求和分档 |
| TC-RAG-EXT-051 | /knowledge-gaps/cluster-drafts | 与 api 消费端 zod schema 字段对齐 | P0 | 一致性 | 同上 | 一次成功的聚类请求 | 1) 发送请求 2) 按 `api/src/services/rag/schemas.ts` 的 `ragKnowledgeGapClusterResultSchema` 逐字段比对 | HTTP 200；顶层含 `schema_version/clusters/ignored_count/metadata`；每簇必含 `cluster_id/title/representative_question/normalized_key/gap_type/severity/frequency_count` 七个必填项（类型分别为 string×6、number×1）；可选数组字段 `sample_failed_question_ids/questions/event_types/related_terms/retrieval_evidence/term_candidates/alias_candidates/faq_drafts/knowledge_card_drafts/document_supplement_suggestions` 均为 array；全字段 snake_case | 无（HTTP 层与 zod 层均无用例；api 侧解析在 `parseRagContract("RAG knowledge gap cluster result", ...)`） | 已实现 | 逐字段核对 rag `KnowledgeGapClusterDraft` 与 api zod schema：七个必填项名称与类型一致；api `endpoints.ts` 发送的请求体亦为 snake_case（`failed_questions`/`min_frequency`/`max_clusters`），与 `KnowledgeGapClusterRequest` 一致 |
| TC-RAG-EXT-052 | /knowledge-gaps/cluster-drafts | gap_type 与 severity 枚举与 api 治理枚举一致 | P0 | 一致性 | 同上 | 分别构造 refusal、low_confidence、user_retry、follow_up_correction、negative_feedback 五类单事件簇，以及混合事件簇 | 1) 逐类发送 2) 校验 `gap_type` 与 `severity` 取值 | HTTP 200；单事件簇 `gap_type` 分别等于该 `event_type` 原值；混合事件簇 `gap_type == "mixed"`；所有 `severity` ∈ `{"low","medium","high","critical"}`。上述取值须与 api `FAILED_QUESTION_EVENT_TYPES`（refusal/low_confidence/user_retry/follow_up_correction/negative_feedback）+ `mixed` 及 `KNOWLEDGE_GAP_SEVERITIES` 完全一致，否则 api 会静默归一为 `mixed`/`medium` | 无（api 侧 `isKnowledgeGapType`/`isKnowledgeGapSeverity` 兜底归一逻辑存在，但无跨服务一致性用例） | 已实现 | 逐值比对：`_EVENT_SEVERITY_WEIGHT` 键集合 = `FAILED_QUESTION_EVENT_TYPES`；`_severity` 返回值集合 = `KNOWLEDGE_GAP_SEVERITIES`，两侧完全对齐 |
| TC-RAG-EXT-053 | /knowledge-gaps/cluster-drafts | 低于 min_frequency 的信号计入 ignored_count | P1 | 边界 | 同上 | 体 `{"failed_questions":[{"id":"a","question":"OCV 异常怎么处理"}],"min_frequency":2}`；以及 `min_frequency` 省略（默认 2） | 1) 两次发送 2) 读取 `clusters` 与 `ignored_count` | HTTP 200；`clusters == []`、`ignored_count == 1`；两次结果一致（默认值同为 2）；`metadata.min_frequency == 2` | `rag/tests/test_knowledge_gap_drafts.py::test_build_knowledge_gap_cluster_drafts_generates_review_candidates`（断言 `ignored_count == 1`，但场景是第 3 条单例信号被忽略，未覆盖“唯一信号被忽略”的分支） | 已实现 | 实测 `{"min_frequency":2,"failed_questions":[1条]} → {"clusters":[],"ignored_count":1}`；`if len(signals) < request.min_frequency: ignored += len(signals)` |
| TC-RAG-EXT-054 | /knowledge-gaps/cluster-drafts | min_frequency 与 max_clusters 越界返回 422 | P1 | 边界 | 同上 | `min_frequency` 取 0 与 21；`max_clusters` 取 0 与 101 | 1) 四次发送 2) 读取 status 与 detail | 四次均 HTTP 422；`detail[0].loc == ["body","min_frequency"]` 或 `["body","max_clusters"]`，`type` 为 `greater_than_equal` 或 `less_than_equal`；边界值 1/20（min_frequency）与 1/100（max_clusters）返回 200 | 无 | 已实现 | 实测 `min_frequency:0 → 422 greater_than_equal ge:1`；模型声明 `Field(default=2, ge=1, le=20)` 与 `Field(default=20, ge=1, le=100)` |
| TC-RAG-EXT-055 | /knowledge-gaps/cluster-drafts | 空请求体返回空簇而非报错 | P1 | 边界 | 同上 | 体 `{}`（三个信号数组均缺省） | 1) 发送请求 2) 校验 `clusters` 与 `metadata` | HTTP 200；`clusters == []`、`ignored_count == 0`；`metadata.input_count == 0`、`failed_question_count == 0`、`manual_note_count == 0`、`high_confidence_answer_count == 0`、`algorithm == "term-intent-bucket/v1"` | 无 | 已实现 | 实测 `{} → 200 {"clusters":[],"ignored_count":0,"metadata":{...全 0...}}`；三个数组字段均有 `default_factory=list` |
| TC-RAG-EXT-056 | /knowledge-gaps/cluster-drafts | 人工笔记与高置信答案并入既有簇 | P0 | 正向 | 同上 | 1 条 `failed_questions`（EOL 拒答）+ 1 条 `manual_notes`（含同术语）+ 1 条 `high_confidence_answers`（含检索证据）；`min_frequency: 2` | 1) 发送请求 2) 校验簇计数与证据 | HTTP 200；`clusters` 长度 1 且 `frequency_count == 3`；`metadata.failed_question_count == 1`、`manual_note_count == 1`、`high_confidence_answer_count == 1`；`term_candidates[0].canonical_term == "EOL"`；`retrieval_evidence[0]["chunk_id"] == "eol-safe"` | `rag/tests/test_knowledge_gap_drafts.py::test_build_knowledge_gap_cluster_drafts_accepts_notes_and_high_confidence_answers` | 已实现 | `_signals_from_context` 把 `manual_notes` 映射为 `event_type="manual_note"`、`high_confidence_answers` 映射为 `event_type="high_confidence_answer"`，再按术语交集并入既有桶 |
| TC-RAG-EXT-057 | /knowledge-gaps/cluster-drafts | 空白 question 信号被计入 ignored_count | P1 | 异常 | 同上 | `failed_questions` 含 2 条 question 为 `""` 与 `"   "`，另 2 条为可聚类正文 | 1) 发送请求 2) 读取 `ignored_count` 与 `metadata.input_count` | HTTP 200；`ignored_count >= 2`（两条空白各计 1）；`metadata.failed_question_count == 4`（原始条数，不因忽略而减少）；不得因空白 question 触发 500 或 422 | 无 | 已实现 | `if not signal.question.strip(): ignored += 1; continue`；`metadata.input_count` 统计原始长度 |
| TC-RAG-EXT-058 | /knowledge-gaps/cluster-drafts | 超过 max_clusters 的簇计入 ignored_count | P2 | 边界 | 同上 | 构造 5 个互不相关的高频术语簇（各 2 条），`max_clusters: 2`、`min_frequency: 2` | 1) 发送请求 2) 统计簇数与忽略数 | HTTP 200；`clusters` 长度 2；`ignored_count == 3`（被截断的簇数）；簇按 `frequency_count` 降序、同频次按 severity 优先级 critical>high>medium>low 排序；`metadata.max_clusters == 2` | 无 | 已实现 | `limited = clusters[:max_clusters]` 后 `ignored += max(0, len(clusters) - len(limited))`；排序键为 `(-frequency_count, severity_rank, title)` |
| TC-RAG-EXT-059 | /knowledge-gaps/cluster-drafts | cluster_id 幂等稳定可跨请求去重 | P1 | 一致性 | 同上 | 同一组失败问题连续请求两次（第 2 次打乱数组顺序） | 1) 发送第一次请求并记录 `cluster_id` 2) 打乱输入顺序再次发送 3) 对比 | 两次均 HTTP 200；同一语义簇的 `cluster_id` 完全相同（`kgc_` + sha1 前 16 位十六进制）；`normalized_key` 亦相同。注意 `_stable_id` 对 `ids` 做了 `sorted()`，故输入顺序不影响结果 | 无（api 侧依赖 `normalized_key` 做 `getKnowledgeGapByKey` 幂等更新） | 已实现 | `_stable_id` 以竖线连接 `key` 与已 sorted 的 `ids` 后取 sha1 前 16 位，加 `kgc_` 前缀；因 ids 先排序，同簇输入顺序不影响结果 |
| TC-RAG-EXT-060 | /knowledge-gaps/cluster-drafts | 检索证据去重与条数上限 | P2 | 边界 | 同上 | 一个簇内 3 条失败问题共提交 15 条 `retrieval_evidence`，其中 5 条 `chunk_id` 重复、1 条所有标识字段为空 | 1) 发送请求 2) 统计 `retrieval_evidence` | HTTP 200；该簇 `retrieval_evidence` 长度 ≤12；按 `chunk_id` > `source_id` > `document_id` > `title` 首值去重后每键仅一条；所有标识字段均为空的那条被丢弃、不产生空元素 | `rag/tests/test_knowledge_gap_drafts.py::test_build_knowledge_gap_cluster_drafts_accepts_notes_and_high_confidence_answers`（仅断言首条 `chunk_id`，未覆盖去重与截断） | 已实现 | `_merge_retrieval_evidence` 以 `by_key` 去重后 `list(by_key.values())[:12]`，`key` 为空则跳过；`snippet` 截断 240 字符 |
| TC-RAG-EXT-061 | /knowledge-gaps/cluster-drafts | 未知 event_type 被原样回显进 gap_type | P2 | 一致性 | 同上 | 同一术语下 2 条 `failed_questions`，`event_type: "custom_signal"`（不在 api 五类枚举内） | 1) 发送请求 2) 读取 `gap_type` 与 `event_types` | HTTP 200；`gap_type == "custom_signal"` 被原样回显到契约中；`event_types == ["custom_signal"]`。api 侧 `isKnowledgeGapType` 会把它归一为 `"mixed"` 后才落库，故最终治理数据不会出错，但 rag 契约层缺少入参校验 | 无 | 部分实现 | `gap_type = event_types[0] if len(event_types) == 1 else "mixed"` 无枚举白名单校验；契约漂移由 api 侧兜底吸收，rag 自身不防御 |
| TC-RAG-EXT-062 | /knowledge-gaps/cluster-drafts | 未携带服务密钥时 401 | P1 | 权限 | `rag_api_key` 已配置 | 无密钥头 / 错误密钥 / 正确密钥 | 1) 逐一发送 2) 记录 status | 无密钥与错误密钥 HTTP 401；正确密钥 HTTP 200 | 无 | 已实现 | 该端点同样挂载 `Depends(verify_api_key)` |
| TC-RAG-EXT-063 | /terminology/contract | 术语契约与内置词条返回结构 | P0 | 正向 | 服务可启动 | GET `/rag/terminology/contract`，无请求体 | 1) 发送请求 2) 校验 `contract` 与 `terms` | HTTP 200；顶层键恰为 `{contract, terms}`；`contract.schema_version == "terminology.v1"`、`contract.owner_service == "api"`、`contract.consumer_service == "rag"`；`terms` 长度为 7，`canonical_term` 集合 == `{"OCV","DCR","EOL","SOC","SOP","CCD","Busbar"}` | `rag/tests/test_query_rewrite.py::test_terminology_contract_covers_initial_battery_line_terms`（函数级调用，未经 HTTP） | 已实现 | 实测返回 `contract` 六键（schema_version/owner_service/consumer_service/required_fields/structured_fields/retrieval_rule）与 7 条 term；`terms` 为全量内置词库，非分页 |
| TC-RAG-EXT-064 | /terminology/contract | 词条满足契约声明的必填与结构化字段 | P1 | 一致性 | 同上 | 同一次响应 | 1) 发送请求 2) 以 `contract.required_fields` 与 `contract.structured_fields` 反过来校验每条 term | HTTP 200；每条 term 均含 `required_fields` 的 `canonical_term`、`definition` 且 `definition` 非空；`structured_fields` 的 `abbreviation/aliases/definition/applicable_scenarios/source_refs/related_topics/retrieval_terms` 键全部存在；`aliases/synonyms/applicable_scenarios/related_topics/retrieval_terms` 为 array，`source_refs` 为对象数组且每项含 `document_id/title/section_path` 三键 | `rag/tests/test_query_rewrite.py::test_terminology_contract_covers_initial_battery_line_terms`（断言 7 个术语的 `definition`/`applicable_scenarios`/`source_refs`/`related_topics`/`retrieval_terms` 非空） | 已实现 | `TermEntry.as_contract_dict()` 输出 10 键，覆盖 `structured_fields` 全部 7 项加 `synonyms`/`source`；`TermSourceRef.as_dict()` 输出三键 |
| TC-RAG-EXT-065 | /terminology/contract | 未携带服务密钥时 401 | P1 | 权限 | `rag_api_key` 已配置 | 无密钥头 / 正确密钥 | 1) 两次发送 2) 记录 status | 无密钥 HTTP 401；正确密钥 HTTP 200；401 时不返回任何术语内容 | 无 | 已实现 | 该 GET 端点挂载 `Depends(verify_api_key)` |
| TC-RAG-EXT-066 | /documents | 已入库文档清单按标题排序并聚合分块统计 | P1 | 正向 | 服务可启动；Chroma collection 已存在 | GET `/rag/documents` | 1) 发送请求 2) 校验每条记录与排序 | HTTP 200；顶层键为 `documents`，值为 array；每项含 `document_id/title/chunk_count/section_paths/category`，`chunk_count` 为整数且等于该文档元数据条数、`section_paths` 内无重复、`category` 为 `section_path` 按 `" / "` 切分后的首段（无 section_path 时为空串）；数组按 `title` 升序 | 无 | 已实现 | 实测返回 `{"documents":[{"document_id":"48e08959-...","title":"CCD AI视觉检测系统","chunk_count":12,"section_paths":["CCD AI视觉检测系统 / 3. 参数说明"],"category":""}, ...]}` |
| TC-RAG-EXT-067 | /documents | Chroma 不可用时 500 且不泄露内部细节 | P2 | 异常 | 令 `_get_collection()` 抛异常 | GET `/rag/documents` | 1) 注入异常 2) 发送请求 3) 读取 detail | HTTP 500；`detail == "Internal server error"`；响应体不含异常类名、文件路径或 Chroma 内部信息 | 无 | 已实现 | `except Exception: logger.exception(...); raise HTTPException(500, "Internal server error")`；与 `/diagram/generate` 的 `detail=str(e)` 形成对比 |
| TC-RAG-EXT-068 | /documents/ingest | ep 形态字节运输入库成功 | P1 | 正向 | 同上；允许目录可写 | 体 `{"namespace":"ep-docs","targetType":"ASSET","targetId":9,"title":"样例","scopes":[],"fileContentBase64":"<base64>","fileName":"规范.txt"}`，头 `X-Service-Key` | 1) 发送请求 2) 校验响应与暂存清理 | HTTP 200；响应含 `document_id/chunk_count/index_status/ok/message`；`ok == true`、`index_status == "ready"`、`document_id == "ep-docs:ASSET:9"`、`chunk_count >= 1`；响应返回后暂存文件已删除 | `rag/tests/test_capability_contract.py::test_ingest_route_accepts_file_content_base64`；`::test_require_ingest_source_base64_stages_into_allowed_dir` | 已实现 | `IngestResult` 的 `ok`/`message` 为 ep 契约增量字段；api 侧 `RagIngestResponse` 未声明这两项，属多余字段（消费者忽略） |
| TC-RAG-EXT-069 | /documents/ingest | 暂存文件在成功与失败两条路径均被清理 | P2 | 边界 | 同上 | 一次成功请求；一次令 `pipeline.ingest_document` 抛异常但暂存已完成的请求 | 1) 两次发送 2) 检查暂存目录 | 两次响应分别 HTTP 200 / HTTP 500；两种情况下暂存目录均无残留 `*-<fileName>` 文件（`finally` 中 `os.remove`）；`os.remove` 失败时不抛出、仅记 debug 日志 | `rag/tests/test_capability_contract.py::test_require_ingest_source_base64_stages_into_allowed_dir`（只断言落盘位置，未断言清理） | 已实现 | 路由 `finally: if temp_path: os.remove(...) except OSError: logger.debug(...)`；`temp_path` 由 `_require_ingest_source` 返回，未落盘时为 None |
| TC-RAG-EXT-070 | /documents/reindex | 重建索引返回 ReindexResult（无 ok/message） | P2 | 一致性 | 同上 | 体 `{"document_id":"doc-1","file_path":"<已入库文件>","metadata":{},"namespace":"","scopes":[]}` | 1) 发送请求 2) 校验字段集合 | HTTP 200；响应键集合恰为 `{document_id, chunk_count, index_status}`，不含 `ok`/`message`；与 API 侧 `RagIngestResponse`（`document_id`/`chunk_count`/`index_status`）一致；文件不存在或越权时 HTTP 400 且 `detail` 为 `ValueError` 文本 | 无（`rag/tests` 无 reindex 用例；api 侧 `reindexDocument` 在 `api/src/services/rag/endpoints.ts`） | 已实现 | `ReindexResult` 仅声明三字段，与 api `RagIngestResponse` 三字段完全一致；与 `/documents/ingest` 的 `IngestResult` 刻意保持差异 |
| TC-RAG-EXT-071 | /search | 检索响应契约与 api 解析一致 | P2 | 一致性 | 服务可启动；Chroma 有数据 | 体 `{"query":"EOL 测试包含哪些项目","top_k":5,"mode":"vector","allowed_security_levels":["public","internal"],"filters":{}}` | 1) 发送请求 2) 校验 `SearchResult` 字段 | HTTP 200；顶层含 `query/expanded_query/term_expansion_hits/rerank_trace/results/latency_ms`；`results[]` 每项含 `chunk_id/document_id/document_title/content/score`（api `ragSearchResultSchema` 的必填项）且 `score` 为 number、`content` 为 string；`latency_ms` 为整数 | 无（`rag/tests` 无 `/rag/search` HTTP 层用例） | 已实现 | 消费者契约为 `api/src/services/rag/schemas.ts` 的 `ragSearchResultSchema`（含 7 个必填字段，`passthrough`）；`SearchResult` 由 `response_model` 强制序列化 |
| TC-RAG-EXT-072 | /search/debug | 调试检索响应契约 | P2 | 一致性 | 同上 | 体 `{"query":"EOL 测试","top_k":8,"include_prompt":true}` | 1) 发送请求 2) 校验 `DebugSearchResult` 字段 | HTTP 200；顶层含 `query/normalized_query/filters/retrieval/prompt_preview/context_chars/estimated_tokens`；`include_prompt == true` 时 `prompt_preview` 非空、`context_chars > 0` 且 `estimated_tokens > 0`；`include_prompt` 缺省（false）时 `prompt_preview == ""` | 无 | 已实现 | `DebugSearchResult` 七字段由 `response_model` 保证；`top_k` 约束 `ge=1, le=50`（默认 8） |
| TC-RAG-EXT-073 | /chat | LLM 预算超限返回 429 与结构化错误体 | P1 | 异常 | LLM 调用抛 `LlmBudgetExceeded` | 体 `{"query":"EOL 测试包含哪些项目","top_k":3}` | 1) 令 LLM 抛预算异常 2) 发送请求 3) 读取 status 与 detail | HTTP 429；`detail.code == "LLM_BUDGET_EXCEEDED"`；`detail` 含 `message/limit_type/limit/used/user_id`；与 `/diagram/generate`、`/chat` 使用同一 `_budget_exceeded_http_error` 构造 | 无 | 已实现 | 路由 `except LlmBudgetExceeded: raise _budget_exceeded_http_error(e)`；与 `/extract` 的 429 死代码形成对比（`/extract` 因 await 前降级而不可达） |
| TC-RAG-EXT-074 | /chat | 内部异常返回固定 500 文案不泄露细节 | P2 | 异常 | 令 `pipeline.chat` 抛非预算异常 | 体 `{"query":"EOL 测试","top_k":3}` | 1) 注入异常 2) 发送请求 3) 读取 detail | HTTP 500；`detail == "Internal server error"`；响应体不含异常类名、堆栈或本地路径 | 无 | 已实现 | `except Exception: logger.exception("Chat failed"); raise HTTPException(500, "Internal server error")` |
| TC-RAG-EXT-075 | /chat/stream | ep 能力服务模式事件序与 citations 契约 | P0 | 一致性 | 请求头带 `X-Service-Key`；LLM 可用 | POST `/rag/chat/stream`，头 `X-Service-Key`，体 `{"query":"EOL 测试包含哪些项目","session_id":"s1"}` | 1) 发送请求 2) 按 SSE 逐事件解析 3) 校验事件名与字段 | HTTP 200 且 `Content-Type: text/event-stream`；事件序严格为 `meta → delta* → citations → done`（拒答时 `meta → delta → citations(refs=[]) → done`）；`meta` 事件含 `sessionId` 与 `messageId`（均可为空串）；`delta` 含 `text`；`citations` 含 `refs` 数组，每项含 `docId/location/excerpt/inScope` 四键、`inScope` 为布尔、`excerpt` ≤200 字符；`done` 含 `usage`；未知内部事件不转发给调用方 | 无（`rag/tests/test_refusal_policy.py` 只覆盖无密钥的存量模式；ep 侧 `HttpAiCapabilityClientTest` 覆盖客户端解析，未与服务端联测） | 存疑 | 按代码核查：`ep_mode = bool(req.headers.get("X-Service-Key"))` 分支完整实现了事件映射与 `_ep_citation_refs`；但未运行验证（依赖真实 LLM 流式），且无任何自动化用例承载该分支 |
| TC-RAG-EXT-076 | /chat/stream | 存量模式事件序保持向后兼容 | P1 | 一致性 | 请求头不带 `X-Service-Key`（可带 `X-API-Key`） | POST `/rag/chat/stream`，体 `{"query":"这个呢？","top_k":3}`（低信息量问句） | 1) 发送请求 2) 按 SSE 逐事件解析 | HTTP 200；事件序为 `stage(accepted) → meta(retrieval_ms/hit_count/query_rewrite) → token* → done`；拒答分支 `done` 事件含 `sources/confidence/followups/answer_ir/visual_plan` 五键；`answer_ir.status == "insufficient_context"`、`answer_ir.confidence == 0.0`；`visual_plan` 含 `artifacts` 与 `warnings` | `rag/tests/test_refusal_policy.py::（第 213 行 POST /rag/chat/stream 拒答分支）` | 已实现 | 无 `X-Service-Key` 时 `ep_mode` 为假，走存量分支：先发 `stage`，`meta` 携带 `retrieval_ms/hit_count/query_rewrite`，`done` 携带完整 `answer_ir` 与 `visual_plan` |
| TC-RAG-RET-001 | 鉴权 | 未配置 RAG_API_KEY 时检索端点免鉴权 | P0 | 权限 | `config.rag_api_key` 为空（`RAG_API_KEY=` 环境变量置空） | 空请求头 | 1. 确保环境变量 RAG_API_KEY 未设置或为空串 2. POST /rag/search 体 {"query":"绝缘电阻测试标准","top_k":3} 3. 不带任何密钥头 | HTTP 200；响应体含 results/latency_ms 字段；不经 401 | rag/tests/test_capability_contract.py::test_auth_skipped_when_key_unconfigured（在 /rag/usage 上断言） | 已实现 | `verify_api_key` 首行 `if not config.rag_api_key: return`（routes.py:78-79），密钥未配置时直接放行 |
| TC-RAG-RET-002 | 鉴权 | X-Service-Key 头通过服务鉴权 | P0 | 权限 | `config.rag_api_key` 已配置为 cap-test-key | 头 `X-Service-Key: cap-test-key` | 1. 设置 RAG_API_KEY=cap-test-key 2. POST /rag/search 带 X-Service-Key: cap-test-key 3. 检查状态码 | HTTP 200；正常返回检索结果，非 401 | rag/tests/test_capability_contract.py::test_auth_accepts_service_key_and_legacy_key（在 /rag/usage 上断言同一 Depends） | 已实现 | routes.py:80 `request.headers.get("X-Service-Key") or request.headers.get("X-API-Key", "")`，服务键优先 |
| TC-RAG-RET-003 | 鉴权 | X-API-Key 存量网关头兼容 | P1 | 权限 | `config.rag_api_key` 已配置 | 头 `X-API-Key: cap-test-key` | 1. 设置 RAG_API_KEY=cap-test-key 2. POST /rag/search 带 X-API-Key: cap-test-key 3. 检查状态码 | HTTP 200；正常返回结果，证明 X-API-Key 与 X-Service-Key 等价 | rag/tests/test_capability_contract.py::test_auth_accepts_service_key_and_legacy_key（在 /rag/usage 上断言同一 Depends） | 已实现 | routes.py:80 双头兼容；docstring routes.py:73-77 说明迁移期两者并存 |
| TC-RAG-RET-004 | 鉴权 | 密钥错误或缺失返回 401 | P0 | 异常 | `config.rag_api_key` 已配置为 cap-test-key | 头 `X-Service-Key: wrong`；或完全不带密钥头 | 1. 设置 RAG_API_KEY=cap-test-key 2. POST /rag/search 带 X-Service-Key: wrong 3. 再 POST 一次不带任何密钥头 | 两次均 HTTP 401；detail 恰为字符串 "Invalid or missing API key" | rag/tests/test_capability_contract.py::test_auth_accepts_service_key_and_legacy_key（在 /rag/usage 上断言同一 Depends） | 已实现 | routes.py:81-82 `if provided != config.rag_api_key: raise HTTPException(401, "Invalid or missing API key")` |
| TC-RAG-RET-005 | 检索 | /rag/search 基本正向检索 | P0 | 正向 | 电池语料已入库到默认 collection（namespace 为空） | {"query":"绝缘电阻测试标准","top_k":5,"allowed_security_levels":["public","internal"]} | 1. POST /rag/search 上述体 2. 读取响应 3. 断言 results 结构与排序 | HTTP 200；body 含 query/expanded_query/term_expansion_hits/rerank_trace/results/latency_ms 六字段；results 为数组且长度 <= 5；每条含 chunk_id/document_id/document_title/content/score；results 按 score 降序 | rag/tests/test_retrieval_quality.py::test_search_expands_terminology_before_embedding_and_records_hits（直调 RagPipeline.search，mock 掉 embed/search）；api/tests/test-search.sh（经 api 代理端到端） | 已实现 | routes.py:335-349 → pipeline/__init__.py:190-247；SearchResult 定义 models.py:167-173，SearchHit 必填字段 models.py:88-104 |
| TC-RAG-RET-006 | 检索 | top_k 缺省值为 5 | P1 | 边界 | 语料 chunk 数 > 5 | {"query":"激光焊接"} 不含 top_k | 1. POST /rag/search 体仅含 query 2. 统计 results 长度 | HTTP 200；results 长度等于 5（或语料不足时等于全部 chunk 数） | 无 | 已实现 | models.py:114 `top_k: int = Field(default=5, ge=1, le=50)` |
| TC-RAG-RET-007 | 检索 | top_k 下边界 1 | P1 | 边界 | 语料非空 | {"query":"激光焊接","top_k":1} | 1. POST /rag/search 体含 top_k=1 2. 统计 results 长度 | HTTP 200；results 长度恰为 1 | 无 | 已实现 | models.py:114 ge=1；pipeline/__init__.py:214-221 rerank_hits 以 top_k 截断 |
| TC-RAG-RET-008 | 检索 | top_k 上边界 50 | P1 | 边界 | 语料 chunk 数 >= 50 | {"query":"焊接","top_k":50} | 1. POST /rag/search 体含 top_k=50 2. 检查状态码与 results 长度 | HTTP 200；results 长度 <= 50；不触发 422 | 无 | 已实现 | models.py:114 le=50；pipeline/__init__.py:203 candidate_top_k = max(top_k, min(top_k*3, 30)) 上限 30 候选 |
| TC-RAG-RET-009 | 检索 | top_k=0 触发请求校验失败 | P0 | 异常 | 无 | {"query":"激光焊接","top_k":0} | 1. POST /rag/search 体含 top_k=0 2. 读取状态码与 detail | HTTP 422；detail 为数组，首个元素的 loc 以 ["body","top_k"] 结尾，type 为 greater_than_equal | 无 | 已实现 | models.py:114 ge=1，FastAPI 在进入 handler 前拦截（routes.py:336 的 try 不覆盖校验错误） |
| TC-RAG-RET-010 | 检索 | top_k=51 超出上限 | P1 | 异常 | 无 | {"query":"激光焊接","top_k":51} | 1. POST /rag/search 体含 top_k=51 2. 读取状态码与 detail | HTTP 422；detail 首个元素 loc 以 ["body","top_k"] 结尾，type 为 less_than_equal | 无 | 已实现 | models.py:114 le=50 |
| TC-RAG-RET-011 | 检索 | 缺少必填 query 字段 | P0 | 异常 | 无 | {"top_k":5} | 1. POST /rag/search 体不含 query 2. 读取状态码与 detail | HTTP 422；detail 首个元素 loc 为 ["body","query"]，type 为 missing | 无 | 已实现 | models.py:113 `query: str`（无默认值、无 validation_alias，与 ChatRequest 不同） |
| TC-RAG-RET-012 | 检索 | 空 query 字符串不报错但结果可判空 | P1 | 边界 | 语料非空 | {"query":"","top_k":5} | 1. POST /rag/search 体 query 为空串 2. 记录状态码与 results 长度 3. 记录 expanded_query | HTTP 200（无 min_length 校验）；expanded_query 亦为空串；results 为数组（可能为空或全量按向量距离排序） | 无 | 已实现 | models.py:113 无 min_length；term_expansion 对空串不抛错（terminology.expand_query_with_terms） |
| TC-RAG-RET-013 | 检索 | 超长 query（10 万字符）不导致 500 | P2 | 边界 | 服务已启动 | {"query":"焊接" 重复 5 万次,"top_k":5} | 1. POST /rag/search 体含超长 query 2. 检查状态码与 latency_ms | HTTP 200；不返回 500；latency_ms 为整数（无长度上限保护，可能显著变慢） | 无 | 已实现 | models.py:113 无 max_length；term_expansion/embed_query 均无长度裁剪，仅 embedding 可能变慢 |
| TC-RAG-RET-014 | 检索 | 空语料 collection 返回空结果 | P0 | 边界 | 存在一个已创建但 0 chunk 的 collection | {"query":"激光焊接","top_k":5,"namespace":"ret-empty-ns"} | 1. 用未入库过的新 namespace 调 POST /rag/search 2. 读取 results | HTTP 200；results 恰为空数组 []；latency_ms 存在；不返回 404 或 500 | 无 | 已实现 | vector_store.py:183-184 `if col.count() == 0: return []`；pipeline/__init__.py:214-221 rerank_hits 对空 hits 走 early return（retrieve.py:318-326） |
| TC-RAG-RET-015 | 检索 | 不存在的 namespace 被隐式创建 | P1 | 边界 | 之前从未使用过该 namespace | {"query":"激光焊接","namespace":"brand-new-ns-xyz"} | 1. POST /rag/search 带 namespace=brand-new-ns-xyz 2. 检查状态码 3. GET /rag/health 观察 chroma_count 变化 | HTTP 200；results 为空数组；服务端通过 get_or_create_collection 新建 ns_brand_new_ns_xyz，不报错 | rag/tests/test_capability_contract.py::test_namespace_resolution_ep_docs_and_explicit_mapping（只断言纯函数返回值） | 已实现 | vector_store.py:74-77 `get_or_create_collection`；vector_store.py:60 非默认 namespace 生成 `ns_<safe>` |
| TC-RAG-RET-016 | 检索 | namespace 全为特殊字符时静默回退默认语料 | P1 | 边界 | 特殊字符 namespace | {"query":"激光焊接","namespace":"---"} | 1. POST /rag/search 带 namespace="---" 2. 观察返回的 results 是否来自电池语料 3. 对照 namespace="" 的同一请求 | 两次请求返回完全相同的 results（同一 collection battery_line_knowledge_v1）；不报错、不隔离 | 无 | 部分实现 | vector_store.py:55-60 safe 净化后为空串 → `return config.chroma_collection`；垃圾 namespace 会无声读到电池语料，缺少显式拒绝 |
| TC-RAG-RET-017 | 检索 | filters.document_id 精确过滤生效与对照 | P0 | 正向 | 语料中存在 doc A 与 doc B | {"query":"焊接","filters":{"document_id":"<docA>"}} 与 {} 对照 | 1. POST /rag/search 带 filters.document_id=docA 2. 断言所有 results 的 document_id 均为 docA 3. 再不带 filters 发同一 query 4. 断言第二次结果包含非 docA 文档 | HTTP 200；带 filter 时 results 全部 document_id == docA；不带 filter 时出现 docA 以外的 document_id | 无 | 已实现 | vector_store.py:191-192 `where["document_id"] = filters["document_id"]`；retrieve.py:248-256 filter_match_score 参与 rerank |
| TC-RAG-RET-018 | 检索 | filters.category 过滤生效 | P1 | 正向 | 语料中 category 字段非空 | {"query":"焊接","filters":{"category":"工艺"}} | 1. POST /rag/search 带 filters.category 2. 断言 results 全部 category 等于该值 | HTTP 200；results 中每条 metadata.category 均等于 "工艺"；若语料无该 category 则 results 为空数组 | 无 | 已实现 | vector_store.py:190-191；category 来源见 pipeline/__init__.py:145 `"category": meta.get("category","")` |
| TC-RAG-RET-019 | 检索 | filters.tags 走子串匹配（非集合成员语义） | P1 | 边界 | 某 chunk 的 tags 落库为逗号连接字符串 | {"query":"焊接","filters":{"tags":"安全"}} | 1. 入库一篇 tags 含 "激光安全" 的文档 2. POST /rag/search 带 filters.tags="安全" 3. 断言该文档被召回 4. 另测 filters.tags="安全,环保" 是否命中 | HTTP 200；单值 "安全" 命中该文档（Chroma $contains 为子串语义，非数组包含）；组合值 "安全,环保" 不命中 | 无 | 部分实现 | vector_store.py:194-195 `where["tags"] = {"$contains": filters["tags"]}`；写入侧 vector_store.py:154-158 把 tags 数组 join 成 "," 连接字符串，子串语义会产生跨 tag 误匹配 |
| TC-RAG-RET-020 | 检索 | allowed_security_levels 过滤生效 | P0 | 权限 | 语料含 security_level=internal 与 public 的文档 | {"query":"焊接","allowed_security_levels":["public"]} | 1. POST /rag/search 仅允许 public 2. 断言所有 results 的 metadata.security_level 均为 "public" 3. 再以 ["public","internal"] 重发 4. 断言出现 internal 文档 | 第 2 步全部为 public；第 4 步出现 security_level=internal 的文档，两次结果集不同 | 无 | 已实现 | vector_store.py:187-188 `where["security_level"] = {"$in": allowed_security_levels}`；入库默认值 pipeline/__init__.py:145 |
| TC-RAG-RET-021 | 检索 | allowed_security_levels 空数组等于关闭密级过滤 | P0 | 权限 | 语料含 security_level=secret 的文档 | {"query":"焊接","allowed_security_levels":[]} | 1. POST /rag/search 传 allowed_security_levels=[] 2. 观察是否召回 secret 级文档 3. 与 ["public"] 的结果集对比 | HTTP 200；results 中出现 security_level 非 public/internal 的文档（如 secret）；与受限请求结果集不同 | 无 | 部分实现 | vector_store.py:187 `if allowed_security_levels:` 空列表直接跳过 where 条件 → 无密级过滤，默认值 ["public","internal"] 只在请求省略该字段时生效（models.py:117），显式传 [] 会放开全部密级 |
| TC-RAG-RET-022 | 检索 | 请求 mode 字段被静默忽略 | P1 | 边界 | 语料非空 | 同一 query 分别传 mode="vector"、mode="keyword"、mode="garbage" | 1. POST /rag/search 三次，仅 mode 不同 2. 比对三次 rerank_trace.mode 与 results 顺序 | 三次响应结构一致；rerank_trace.mode 取决于服务端 RAG_RERANK_MODE 而非请求 mode；业务结果无差异 | 无 | 部分实现 | models.py:115 定义了 mode，但 routes.py:338-345 未把 mode 传给 pipeline.search，pipeline.search 签名（pipeline/__init__.py:190-198）也无 mode 参数 |
| TC-RAG-RET-023 | 检索 | rerank_trace 审计结构完整 | P1 | 正向 | 语料非空，top_k >= 2 | {"query":"OCV异常处理","top_k":5} | 1. POST /rag/search 2. 断言 rerank_trace 含 mode/configured_mode/fallback_reason/candidate_count/requested_top_k/topk_distribution/hit_features 3. 断言 hit_features 每项含 rank/original_rank/matched_features | HTTP 200；rerank_trace.requested_top_k 等于请求 top_k；candidate_count 等于向量候选数；topk_distribution 含 count/score_min/score_max/score_avg/score_buckets/documents | rag/tests/test_retrieval_quality.py::test_rerank_adapter_records_topk_distribution_and_hit_features | 已实现 | retrieve.py:684-711 `_build_rerank_trace`；routes.py:340 返回 rerank_trace 到 SearchResult.rerank_trace |
| TC-RAG-RET-024 | 检索 | results 中 metadata.rerank 与原始排名回填 | P2 | 正向 | 语料非空且 rerank 改变了顺序 | {"query":"绝缘电阻测试标准","top_k":5} | 1. POST /rag/search 2. 取 results[0] 与 results[1] 3. 读取 metadata.rerank 各字段 | 每条 results[i] 的 metadata.rerank.rank == i+1；含 mode/original_rank/rank_delta/candidate_count/matched_features；rank_delta == original_rank - rank | rag/tests/test_retrieval_quality.py::test_rerank_adapter_records_topk_distribution_and_hit_features（断言 metadata.rerank.topk_distribution） | 已实现 | retrieve.py:714-732 `_annotate_rerank_metadata` |
| TC-RAG-RET-025 | 检索 | Chroma 不可用时返回 500 与固定 detail | P1 | 异常 | 故意让 `_get_collection` 抛异常（如 CHROMA_PERSIST_DIR 指向只读路径） | {"query":"焊接","top_k":5} | 1. 破坏 Chroma 持久化目录可写性 2. POST /rag/search 3. 读取状态码与 detail | HTTP 500；detail 恰为 "Internal server error"；服务端日志出现 "Search failed" 异常栈 | 无 | 已实现 | routes.py:347-349 `except Exception: logger.exception("Search failed"); raise HTTPException(500, "Internal server error")` |
| TC-RAG-RET-026 | scopes | scopes 空数组不施加过滤（对照） | P0 | 边界 | 语料含多 productLine 文档 | {"query":"焊接","scopes":[]} | 1. POST /rag/search 带 scopes=[] 2. 断言 results 的 productLine 取值不唯一 3. 与带 scopes 的请求对比结果集 | HTTP 200；results 跨越多个 productLine；与 {"scopes":[{"productLine":"L1"}]} 结果集不同 | rag/tests/test_capability_contract.py::test_scope_where_empty_means_unrestricted（只覆盖纯函数 _scope_where） | 已实现 | vector_store.py:349-350 无有效 group 时返回 None；vector_store.py:197-199 仅在 scope_where 非空时合并 where |
| TC-RAG-RET-027 | scopes | 单维度 scopes 过滤生效 | P0 | 正向 | 语料含 productLine=L1 与 L2 的文档，入库时 scopes 已折入 chunk metadata | {"query":"焊接","scopes":[{"productLine":"L1"}]} | 1. 入库 docA scopes=[{"productLine":"L1"}]、docB scopes=[{"productLine":"L2"}] 2. POST /rag/search 带 scopes=[{"productLine":"L1"}] 3. 断言 results 只含 docA 4. 去掉 scopes 重发，断言出现 docB | HTTP 200；带 scopes 时 results 全部 productLine 数组含 "L1"；不带 scopes 时出现 docB | rag/tests/test_capability_contract.py::test_scope_where_and_within_object_or_across_objects（只断言 where 子句字面量） | 已实现 | 写入 vector_store.py:149-152 以数组落库；过滤 vector_store.py:344 `{key: {"$contains": value}}`；折入 pipeline/__init__.py:99-113 |
| TC-RAG-RET-028 | scopes | 同一 scope 对象内多维度为 AND | P0 | 正向 | docA=(L1,B2)、docB=(L1,B3)、docC=(L2,B2) | {"query":"焊接","scopes":[{"productLine":"L1","base":"B2"}]} | 1. POST /rag/search 带双维度同一对象 2. 断言 results 只含 docA | HTTP 200；results 仅含同时满足 productLine 含 L1 且 base 含 B2 的 chunk；docB、docC 均不出现 | rag/tests/test_capability_contract.py::test_scope_where_and_within_object_or_across_objects（只断言 $and 字面量） | 已实现 | vector_store.py:344-348 多维度生成 `{"$and": [...]}` |
| TC-RAG-RET-029 | scopes | 多个 scope 对象之间为 OR | P0 | 正向 | docA=(L1)、docB=(L2)、docC=(L3) | {"query":"焊接","scopes":[{"productLine":"L1"},{"productLine":"L2"}]} | 1. POST /rag/search 带两个 scope 对象 2. 断言 results 含 docA 与 docB 3. 断言不含 docC | HTTP 200；results 覆盖 L1 与 L2 两个取值；docC（L3）不出现 | rag/tests/test_capability_contract.py::test_scope_where_and_within_object_or_across_objects（只断言 $or 字面量） | 已实现 | vector_store.py:351-353 多 group 生成 `{"$or": groups}` |
| TC-RAG-RET-030 | scopes | 全空 scope 对象视为不过滤 | P1 | 边界 | 语料含多 productLine | {"query":"焊接","scopes":[{}]} | 1. POST /rag/search 带 scopes=[{}] 2. 断言 results 跨多个 productLine 3. 与 scopes=[] 的响应逐条比对 | HTTP 200；两次响应 results 完全一致；不返回空数组 | rag/tests/test_capability_contract.py::test_scope_where_empty_means_unrestricted（覆盖 [{}] 与全空值字典） | 已实现 | vector_store.py:335-343 dims 为空则 `continue`；vector_store.py:349-350 groups 为空返回 None |
| TC-RAG-RET-031 | scopes | scopes 与 security/filters 合并为 AND | P0 | 正向 | 语料含 public+L1、internal+L1、internal+L2 | {"query":"焊接","allowed_security_levels":["public"],"filters":{"document_id":"<docA>"},"scopes":[{"productLine":"L1"}]} | 1. POST /rag/search 同时传三组条件 2. 断言 results 每条同时满足密级为 public、document_id 为 docA、productLine 含 L1 | HTTP 200；results 为三者交集；任一条不满足即视为失败 | rag/tests/test_capability_contract.py::test_scope_where_and_within_object_or_across_objects（仅断言 _merge_where 字面量） | 已实现 | vector_store.py:356-362 `_merge_where` 生成 `{"$and": [base, extra]}`；调用点 vector_store.py:197-199 |
| TC-RAG-RET-032 | scopes | 非 SCOPE_DIMENSION_KEYS 的 scope 键被静默忽略 | P1 | 边界 | 语料含多 productLine | {"query":"焊接","scopes":[{"unknownDim":"X"}]} | 1. POST /rag/search 带未知维度键 2. 断言 results 数量与 scopes=[] 时一致 | HTTP 200；未知维度被丢弃，等价于不过滤（results 与 scopes=[] 相同）；不返回 422/400（调用方拼错维度会静默放开权限范围） | 无 | 部分实现 | vector_store.py:338-341 仅保留 `key in SCOPE_DIMENSION_KEYS` 的键；SCOPE_DIMENSION_KEYS 定义 vector_store.py:26-33，无未知键告警 |
| TC-RAG-RET-033 | scopes | 混合候选词法路绕过 scopes 过滤 | P0 | 一致性 | 开启 RAG_HYBRID_CANDIDATES（默认 true），语料含超出指定 scope 的文档 | {"query":"<仅命中范围外文档的词>","top_k":9,"scopes":[{"productLine":"L1"}]} | 1. 入库 docOut scopes=[{"productLine":"L9"}]，内容含独特词 KEYWORD_OUT 2. POST /rag/search query 含 KEYWORD_OUT 且 scopes 仅 L1 3. 断言 results 中不含 docOut | 期望 results 中不含 docOut；实际可能因词法候选路再落入 docOut（超出调用方授权范围），属越权召回 | rag/tests/test_hybrid_candidates.py::test_rrf_merges_and_dedupes_by_chunk_id 等（仅覆盖 RRF 纯函数，不覆盖 scopes） | 存疑 | pipeline/__init__.py:228-236 调 `fuse_lexical_candidates(..., vector_hits=[], allowed_security_levels=..., namespace=...)`，未传 scopes/filters；retrieve.py:407-469 该函数只按 namespace 与 security_level 取全量 active chunk，且 vector_hits=[] 使 vector_docs 为空、去重失效，最多补入 add_cap 条范围外文档后经 RRF 混入结果 |
| TC-RAG-RET-034 | 检索调试 | /rag/search/debug 基本正向（include_prompt=false） | P0 | 正向 | 语料非空 | {"query":"焊接参数","top_k":8,"include_prompt":false} | 1. POST /rag/search/debug 2. 读取响应字段 | HTTP 200；body 含 query/normalized_query/filters/retrieval/prompt_preview/context_chars/estimated_tokens；prompt_preview 为空串 ""；context_chars 与 estimated_tokens 均为 0 | api/tests/test-debug-search.sh（经 api 代理含 include_prompt=true 的端到端） | 已实现 | routes.py:352-367 → pipeline/__init__.py:249-299；prompt 分支仅在 include_prompt 且 hits 非空时进入（pipeline/__init__.py:273） |
| TC-RAG-RET-035 | 检索调试 | include_prompt=true 输出 prompt 预览与上下文统计 | P0 | 正向 | 语料非空且 query 有命中 | {"query":"绝缘电阻测试标准","top_k":5,"include_prompt":true} | 1. POST /rag/search/debug 2. 断言 prompt_preview 非空且含 "[system]" 或 "[user]" 前缀 3. 断言 context_chars > 0 4. 断言 estimated_tokens == context_chars // 2 | HTTP 200；prompt_preview 非空串；context_chars 等于各 hit content 长度之和；estimated_tokens 恰为 context_chars 整除 2 | api/tests/test-debug-search.sh（断言 retrieval.mode、latency_ms、context_chars>0、prompt_preview 存在） | 已实现 | pipeline/__init__.py:273-282 build_messages 后按 role 拼接，单条 content 超 500 字符截断并加 "..."；context_chars = sum(len(content))，estimated_tokens = context_chars // 2 |
| TC-RAG-RET-036 | 检索调试 | debug top_k 缺省值为 8（区别于 /search 的 5） | P1 | 边界 | 语料 chunk 数 > 8 | {"query":"焊接"} 不含 top_k | 1. POST /rag/search/debug 仅含 query 2. 断言 retrieval.top_k == 8 且 results 长度 <= 8 | HTTP 200；retrieval.top_k 恰为 8 | 无 | 已实现 | models.py:182 `top_k: int = Field(default=8, ge=1, le=50)`；回显见 pipeline/__init__.py:290 |
| TC-RAG-RET-037 | 检索调试 | debug top_k=0 触发 422 | P1 | 异常 | 无 | {"query":"焊接","top_k":0} | 1. POST /rag/search/debug 体含 top_k=0 2. 读取状态码与 detail | HTTP 422；detail 首个元素 loc 以 ["body","top_k"] 结尾 | 无 | 已实现 | models.py:182 ge=1 |
| TC-RAG-RET-038 | 检索调试 | retrieval.mode 恒为 hybrid，与请求 mode 无关 | P1 | 边界 | 语料非空 | {"query":"焊接","mode":"vector","top_k":3} | 1. POST /rag/search/debug 传 mode="vector" 2. 断言 retrieval.mode 的值 | HTTP 200；retrieval.mode 恰为 "hybrid"（硬编码），不随请求 mode="vector" 变化 | api/tests/test-debug-search.sh（只断言 mode 非空，未断言取值） | 部分实现 | pipeline/__init__.py:289 `"mode": "hybrid"` 为字面量硬编码；models.py:183 的 mode 从未被读取 |
| TC-RAG-RET-039 | 检索调试 | 无命中时 include_prompt=true 不报错 | P1 | 边界 | 空语料 namespace | {"query":"激光焊接","top_k":3,"include_prompt":true} | 1. 用空 namespace 调 POST /rag/search/debug 2. 读取各字段 | HTTP 200；retrieval.results 为空数组；prompt_preview 为空串；context_chars 与 estimated_tokens 均为 0 | 无 | 已实现 | pipeline/__init__.py:273 `if include_prompt and hits:` 命中为空时整段跳过 |
| TC-RAG-RET-040 | 检索调试 | filters 原样回显 | P2 | 正向 | 无 | {"query":"焊接","top_k":3,"filters":{"document_id":"docA","category":"工艺"}} | 1. POST /rag/search/debug 2. 读取 filters 字段 | HTTP 200；filters 恰为 {"document_id":"docA","category":"工艺"}；未传 filters 时回显 {} | 无 | 已实现 | pipeline/__init__.py:287 `"filters": filters or {}` |
| TC-RAG-RET-041 | 入库 | file_path 形态正向入库 | P0 | 正向 | `RAG_ALLOWED_DIRS` 含 data/uploads，待入库 txt 位于该目录 | 请求 {"document_id":"doc-battery-001","file_path":"<allowed>/a.txt","metadata":{"title":"分选规范","category":"工艺"}} | 1. POST /rag/documents/ingest 上述体 2. 读取响应 3. GET /rag/documents 确认出现该文档 | HTTP 200；body 含 document_id="doc-battery-001"、chunk_count > 0、index_status="ready"、ok=true、message=""；随后列表接口出现该 document_id | rag/tests/test_source_metadata.py::test_html_parser_and_chunks_keep_source_format_metadata（只覆盖 parser+chunker 直调）；api/tests/test-upload.sh（经 api 上传端到端） | 已实现 | routes.py:252-278 → pipeline/__init__.py:120-178；IngestResult 字段 models.py:42-48 |
| TC-RAG-RET-042 | 入库 | base64 字节运输形态正向入库（ep 契约） | P0 | 正向 | `RAG_ALLOWED_DIRS` 已配置且可写 | {"namespace":"ep-docs","targetType":"DOCUMENT","targetId":91,"title":"分选规范","scopes":[{"productLine":"L1"}],"fileContentBase64":"<base64>","fileName":"分选规范.txt"} | 1. POST /rag/documents/ingest 上述体 2. 读取 document_id 3. 检查暂存目录 | HTTP 200；document_id 恰为 "ep-docs:DOCUMENT:91"；chunk_count > 0；暂存目录 capability-staging/ep-docs 下无残留文件 | rag/tests/test_capability_contract.py::test_ingest_route_accepts_file_content_base64（mock 掉 pipeline.ingest_document，仅验源解析与清理） | 已实现 | routes.py:223-249 document_id 回退拼接 `f"{namespace or 'ep'}:{target_type or 'doc'}:{target_id}"`（routes.py:233）；暂存 routes.py:205-220；finally 清理 routes.py:273-278 |
| TC-RAG-RET-043 | 入库 | document_id 与 targetId 同时提供时 document_id 优先 | P1 | 边界 | allowed 目录内有文件 | {"document_id":"explicit-id","namespace":"ep-docs","targetType":"DOCUMENT","targetId":99,"file_path":"<allowed>/a.txt"} | 1. POST /rag/documents/ingest 同时给 document_id 与 targetId 2. 读取响应 document_id | HTTP 200；document_id 恰为 "explicit-id"，不拼接为 "ep-docs:DOCUMENT:99" | 无 | 已实现 | routes.py:231-233 `if not document_id and request.target_id is not None:` 仅在 document_id 为空时才回退拼接 |
| TC-RAG-RET-044 | 入库 | 既无 file_path 也无 base64 返回 400 | P0 | 异常 | 无 | {"document_id":"doc-x"} | 1. POST /rag/documents/ingest 仅含 document_id 2. 读取状态码与 detail | HTTP 400；detail 恰为 "缺少可解析文件：请提供 file_path，或 ep 契约的 fileContentBase64 + fileName" | rag/tests/test_capability_contract.py::test_require_ingest_source_file_path_shape（以 pytest.raises 断言 400，未断言 detail 文案） | 已实现 | routes.py:239-243；注意该分支在 document_id 校验之前（routes.py:244-248），两者都缺时报的是文件缺失 |
| TC-RAG-RET-045 | 入库 | 无 document_id 且无 targetId 返回 400 | P0 | 异常 | allowed 目录内有文件 | {"file_path":"<allowed>/a.txt"} | 1. POST /rag/documents/ingest 仅含 file_path 2. 读取状态码与 detail | HTTP 400；detail 恰为 "document_id 缺失：请提供 document_id 或 ep 契约的 targetId" | 无 | 已实现 | routes.py:244-248 |
| TC-RAG-RET-046 | 入库 | file_path 位于白名单外被拒绝 | P0 | 异常 | `RAG_ALLOWED_DIRS` 为默认 data/uploads 与 knowledge | {"document_id":"doc-evil","file_path":"/etc/passwd"} | 1. POST /rag/documents/ingest 指向 /etc/passwd 2. 读取状态码与 detail | HTTP 400；detail 以 "Path outside allowed directories: " 开头并回显路径；不返回文件内容 | 无 | 已实现 | routes.py:267-269 ValueError → 400；pipeline/__init__.py:62-74 `_validate_file_path` 用 resolve + is_relative_to 双重校验 |
| TC-RAG-RET-047 | 入库 | file_path 指向不存在文件返回 400 | P1 | 异常 | 无 | {"document_id":"doc-x","file_path":"<allowed>/not-exist.txt"} | 1. POST /rag/documents/ingest 2. 读取状态码与 detail | HTTP 400；detail 以 "File not found or not a regular file: " 开头 | 无 | 已实现 | pipeline/__init__.py:70-71 `if not resolved.is_file(): raise ValueError(...)` |
| TC-RAG-RET-048 | 入库 | 未注册扩展名返回 400 且指明扩展名 | P0 | 异常 | allowed 目录内有 .xyz 文件 | {"document_id":"doc-x","file_path":"<allowed>/a.xyz"} | 1. 在 allowed 目录创建 a.xyz 2. POST /rag/documents/ingest 3. 读取状态码与 detail | HTTP 400；detail 恰为 "No parser registered for extension: .xyz" | 无 | 已实现 | parsers/base.py ParserRegistry.get 抛 ValueError；注册表 pipeline/__init__.py:55-92 仅含 md/markdown/txt/pdf/docx/html/htm 与代码类扩展名；routes.py:267-269 转为 400 |
| TC-RAG-RET-049 | 入库 | base64 非法字符串返回 400 | P0 | 异常 | 无 | {"namespace":"ep-docs","targetId":10,"fileContentBase64":"!!!not-base64!!!","fileName":"a.txt"} | 1. POST /rag/documents/ingest 2. 读取状态码与 detail | HTTP 400；detail 恰为 "fileContentBase64 不是合法的 Base64 内容" | rag/tests/test_capability_contract.py::test_require_ingest_source_base64_validation（以 pytest.raises(ValueError) 断言，未走 HTTP 层） | 已实现 | routes.py:184-186 `base64.b64decode(..., validate=True)` 失败抛 ValueError → routes.py:267-269 转 400 |
| TC-RAG-RET-050 | 入库 | base64 解码为空字节返回 400 | P1 | 异常 | 无 | {"namespace":"ep-docs","targetId":11,"fileContentBase64":"","fileName":"a.txt"} | 1. POST /rag/documents/ingest 2. 读取状态码与 detail | HTTP 400；detail 恰为 "fileContentBase64 为空" | 无 | 已实现 | routes.py:187-188 `if not raw: raise HTTPException(400, "fileContentBase64 为空")`（空串解码为 b""） |
| TC-RAG-RET-051 | 入库 | 缺 fileName 返回 400 | P0 | 异常 | 无 | {"namespace":"ep-docs","targetId":11,"fileContentBase64":"<base64>"} 不含 fileName | 1. POST /rag/documents/ingest 2. 读取状态码与 detail | HTTP 400；detail 恰为 "缺少 fileName：base64 运输需要原文件名以推断解析格式" | rag/tests/test_capability_contract.py::test_require_ingest_source_base64_validation（覆盖 no_name 场景，断言 400） | 已实现 | routes.py:192-197 |
| TC-RAG-RET-052 | 入库 | fileName 无扩展名返回 400 | P0 | 异常 | 无 | {"namespace":"ep-docs","targetId":12,"fileContentBase64":"<base64>","fileName":"规范"} | 1. POST /rag/documents/ingest 2. 读取状态码与 detail | HTTP 400；detail 恰为 "fileName 缺少扩展名：无法选择解析器，请提供带扩展名的文件名" | rag/tests/test_capability_contract.py::test_require_ingest_source_base64_validation（覆盖 no_ext 场景，断言 400） | 已实现 | routes.py:198-203 `Path(file_name).suffix.lower()` 为空即拒绝 |
| TC-RAG-RET-053 | 入库 | base64 超过 50MB 上限返回 400 | P2 | 异常 | 无 | fileContentBase64 编码一段 > 50*1024*1024 字节的内容 | 1. 构造解码后 50MB+1 字节的 base64 2. POST /rag/documents/ingest 3. 读取状态码与 detail | HTTP 400；detail 恰为 "fileContentBase64 超过 50MB 大小上限"；服务端不在暂存目录落盘 | 无 | 已实现 | routes.py:174 `_CAPABILITY_UPLOAD_MAX_BYTES = 50 * 1024 * 1024`；routes.py:189-190 在落盘前检查 |
| TC-RAG-RET-054 | 入库 | 内容过短的文档入库成功但 chunk_count=0 | P1 | 边界 | allowed 目录内有 30 字节 txt | {"document_id":"doc-tiny","file_path":"<allowed>/tiny.txt"} | 1. 写入内容仅 30 个字符的 txt 2. POST /rag/documents/ingest 3. 读取 chunk_count 与 index_status | HTTP 200；chunk_count 恰为 0；index_status 仍为 "ready"（不报失败）；随后 /rag/search 检索不到该文档 | 无 | 部分实现 | chunker.py:110-113 末块仅在 `current_len >= MIN_CHUNK_SIZE`（chunker.py:25 = 120）时产出；chunker.py:36-37 空文本返回空列表；upsert_chunks 对空列表返回 0（vector_store.py:85-86）；index_status 硬编码 "ready"（pipeline/__init__.py:177） |
| TC-RAG-RET-055 | 入库 | 单个超长段落不被硬切分 | P1 | 边界 | allowed 目录内有 5000 字符无空行的 txt | {"document_id":"doc-long","file_path":"<allowed>/long.txt"} | 1. 写入 5000 字符单段落文本 2. POST /rag/documents/ingest 3. 读取 chunk_count 4. 取该文档 chunk 的 content 长度 | 期望按 MAX_CHUNK_SIZE=1000 切分；实际可能只产出 1 个远超 1000 字符的 chunk，chunk_count 为 1 | 无 | 存疑 | chunker.py:80-89 仅在 `current_len >= MIN_CHUNK_SIZE` 时才因超长而 flush，首段 current_len=0 会直接追加；chunker.py:91-107 的 while 循环要求 `len(current_chunk) > 1`，单段无法再切，故超长单段落不设上限 |
| TC-RAG-RET-056 | 入库 | metadata 透传并折入 chunk metadata | P1 | 正向 | allowed 目录内有文件 | {"document_id":"doc-meta","file_path":"<allowed>/a.txt","metadata":{"title":"分选规范","category":"工艺","security_level":"public","version":"v2.0"}} | 1. POST /rag/documents/ingest 2. POST /rag/search 该文档内容 3. 读取命中项的 metadata 字段 | HTTP 200；命中项 metadata 中 category 为 "工艺"、version 为 "v2.0"、security_level 为 "public"（未提供时默认为 "internal"） | rag/tests/test_source_metadata.py::test_ingest_metadata_records_preview_source_format_for_standard_formats（只覆盖 _build_ingest_metadata 纯函数） | 已实现 | pipeline/__init__.py:129 `_build_ingest_metadata`；pipeline/__init__.py:141-155 逐字段写入 chunk.metadata；落库 vector_store.py:118-146 |
| TC-RAG-RET-057 | 入库 | scopes 折入 chunk metadata 为数组值 | P0 | 正向 | allowed 目录内有文件 | {"document_id":"doc-scope","file_path":"<allowed>/a.txt","scopes":[{"productLine":"L1","base":"B2"},{"productLine":"L3"}]} | 1. POST /rag/documents/ingest 带两个 scope 对象 2. POST /rag/search 命中该文档 3. 读取 metadata.productLine | HTTP 200；metadata.productLine 为数组 ["L1","L3"]（并集去重排序）；metadata.base 为 ["B2"]；无值维度不出现在 metadata 中 | rag/tests/test_capability_contract.py::test_scope_where_and_within_object_or_across_objects（只覆盖 where 子句构造，不覆盖写入侧） | 已实现 | pipeline/__init__.py:99-113 `_scope_metadata_dims` 按维度取并集并 `sorted`；pipeline/__init__.py:156-159 逐维度回填；vector_store.py:149-152 转 list 落库 |
| TC-RAG-RET-058 | 入库 | 暂存文件在响应后被清理 | P1 | 一致性 | 使用 base64 运输 | {"namespace":"ep-docs","targetId":90,"fileContentBase64":"<base64>","fileName":"a.txt"} | 1. 记录 capability-staging 目录快照 2. POST /rag/documents/ingest 3. 再次遍历该目录 | HTTP 200；暂存目录中无新增残留文件（finally 已 os.remove） | rag/tests/test_capability_contract.py::test_ingest_route_accepts_file_content_base64（断言 `not staged.exists()`，覆盖同一 finally） | 已实现 | routes.py:273-278 finally 块清理 temp_path；清理失败仅记 debug 日志不改变响应 |
| TC-RAG-RET-059 | 入库 | 重复 ingest 同一 document_id 幂等不重复 | P0 | 一致性 | allowed 目录内有文件，已入库一次 | 同一 {"document_id":"doc-dup","file_path":"<allowed>/a.txt"} 连发两次 | 1. POST /rag/documents/ingest 第一次，记录 chunk_count 2. 再发完全相同的请求 3. 断言 chunk_count 相同 4. GET /rag/documents 断言该 document_id 只出现一次且 chunk_count 未翻倍 | 两次均 HTTP 200；chunk_count 相等；列表接口中该 document_id 唯一，chunk_count 等于单次值 | 无 | 已实现 | pipeline/__init__.py:161-162 先 `delete_by_document` 再 `upsert_chunks`；chunk_id 由 document_id+索引确定性生成（chunker.py:197-199），保证 upsert 覆盖而非新增 |
| TC-RAG-RET-060 | 入库 | 同 document_id 落在不同 namespace 互不影响 | P0 | 一致性 | 两个 namespace | 同一 document_id="doc-iso"，分别以 namespace="ns-a" 与 namespace="ns-b" 入库 | 1. POST /rag/documents/ingest namespace=ns-a 2. POST /rag/documents/ingest namespace=ns-b 同一 document_id 3. 分别在两个 namespace 下检索 4. 断言互不串语料 | 两次均 HTTP 200；ns-a 检索只命中 ns-a 内容，ns-b 只命中 ns-b 内容；后一次入库不删除前一个 namespace 的 chunk | rag/tests/test_capability_contract.py::test_namespace_resolution_ep_docs_and_explicit_mapping（只断言 collection 名不同） | 已实现 | pipeline/__init__.py:161 `delete_by_document(document_id, namespace=namespace)` 在目标 collection 内删除（vector_store.py:449-456），namespace 隔离由 resolve_collection_name 保证（vector_store.py:40-60） |
| TC-RAG-RET-061 | 重建索引 | /rag/documents/reindex 正向重建 | P0 | 正向 | allowed 目录内有文件且已入库 | {"document_id":"doc-re","file_path":"<allowed>/a.txt","metadata":{"title":"分选规范"}} | 1. 先 ingest 该 document_id 2. POST /rag/documents/reindex 同一 document_id 与 file_path 3. 读取响应 | HTTP 200；body 含 document_id、chunk_count > 0、index_status="ready" | 无 | 已实现 | routes.py:317-332 → pipeline/__init__.py:180-188，reindex_document 直接委托 ingest_document |
| TC-RAG-RET-062 | 重建索引 | 缺 file_path 触发 422 | P0 | 异常 | 无 | {"document_id":"doc-re"} | 1. POST /rag/documents/reindex 体不含 file_path 2. 读取状态码与 detail | HTTP 422；detail 首个元素 loc 为 ["body","file_path"]，type 为 missing（注意与 ingest 的 400 行为不同） | 无 | 已实现 | models.py:53 `file_path: str` 无默认值，必填；ingest 的 IngestRequest.file_path 为可选（models.py:26） |
| TC-RAG-RET-063 | 重建索引 | 缺 document_id 触发 422 | P0 | 异常 | 无 | {"file_path":"<allowed>/a.txt"} | 1. POST /rag/documents/reindex 体不含 document_id 2. 读取状态码与 detail | HTTP 422；detail 首个元素 loc 为 ["body","document_id"]，type 为 missing | 无 | 已实现 | models.py:52 `document_id: str` 无默认值 |
| TC-RAG-RET-064 | 重建索引 | reindex 与 ingest 等价（均先删后写） | P1 | 一致性 | allowed 目录内有文件 | 同一 {"document_id":"doc-eq","file_path":"<allowed>/a.txt"} 分别走 ingest 与 reindex | 1. ingest 记录 chunk_count_1 2. reindex 记录 chunk_count_2 3. 断言两次返回结构可比较（chunk_count 相同、index_status 均为 ready） | 两次 HTTP 200；chunk_count 相等；无残留重复 chunk（列表接口 chunk_count 未翻倍） | 无 | 已实现 | pipeline/__init__.py:188 `return self.ingest_document(...)` 完全委托，无独立逻辑 |
| TC-RAG-RET-065 | 重建索引 | reindex 换文件后旧内容不再被检索到 | P0 | 一致性 | allowed 目录内有 a.txt 与 b.txt | 1. ingest document_id="doc-swap" 指向 a.txt（内容含词 OLDONLY）2. reindex 同一 document_id 指向 b.txt（内容含词 NEWONLY） | 1. ingest a.txt 2. POST /rag/search query=OLDONLY 断言命中 3. POST /rag/documents/reindex 指向 b.txt 4. 再 POST /rag/search query=OLDONLY 5. 再 POST /rag/search query=NEWONLY | 步骤 2 命中；步骤 4 results 为空数组（旧 chunk 已删）；步骤 5 命中新内容 | 无 | 已实现 | pipeline/__init__.py:161 delete_by_document 在 upsert 前执行，保证旧 chunk 清除；chunk_id 基于 document_id+索引，不会因内容变化产生孤儿 |
| TC-RAG-RET-066 | 文档列表 | /rag/documents 基本正向 | P0 | 正向 | 默认 collection 已有入库文档 | 无请求体 | 1. GET /rag/documents 2. 读取 documents 数组 | HTTP 200；body 为 {"documents":[...]}；每项含 document_id/title/chunk_count/section_paths/category 五字段；数组按 title 升序；chunk_count 为正整数 | 无 | 已实现 | routes.py:141-170；排序 routes.py:167 `sorted(docs.values(), key=lambda d: d["title"])`；字段构造 routes.py:156-165 |
| TC-RAG-RET-067 | 文档列表 | 列表接口不支持 namespace 过滤 | P1 | 边界 | 默认语料与 ns 语料各有文档 | 请求 `GET /rag/documents?namespace=ns-a` | 1. 在 namespace="ns-a" 入库一篇文档 2. GET /rag/documents?namespace=ns-a 3. 断言返回的 document_id 中不含该文档 | 期望能按 namespace 列出；实际忽略该 query 参数并只列出默认 battery collection，ns-a 的文档不可见 | 无 | 部分实现 | routes.py:146 调用 `_get_collection()` 未传 namespace，vector_store.py:63 默认参数为 None → resolve_collection_name(None) 得到默认 collection；路由签名 routes.py:142 无任何查询参数声明 |
| TC-RAG-RET-068 | 文档列表 | 空语料返回空数组 | P1 | 边界 | 默认 collection 为空 | 无请求体 | 1. 清空默认 collection（或使用全新建库）2. GET /rag/documents 3. 读取 documents | HTTP 200；documents 恰为空数组 []；returns 而非 404 | 无 | 已实现 | routes.py:148-167 遍历 metadatas，全空时 docs 为空 dict，sorted 后仍为 [] |
| TC-RAG-RET-069 | 文档列表 | 同文档多 chunk 聚合与 section_paths 去重 | P1 | 正向 | 某文档已被切成 >= 3 个 chunk，跨多个章节 | 无请求体 | 1. 入库一篇包含两个以上章节标题的长文档 2. GET /rag/documents 3. 定位该 document_id 4. 检查 chunk_count 与 section_paths | HTTP 200；chunk_count 等于该文档全部 chunk 数；section_paths 为去重后的章节路径数组（无重复项）；category 等于首个 section_path 按 " / " 分割的第一段 | 无 | 已实现 | routes.py:163-165 chunk_count 累加、section_paths 去重追加；routes.py:161 category 取 `section.split(" / ")[0]` |
| TC-RAG-RET-070 | 文档列表 | 文档被删除后不再出现在列表中 | P1 | 一致性 | 已入库文档 doc-del | 无请求体 | 1. ingest document_id="doc-del" 2. GET /rag/documents 确认存在 3. 以同名 document_id reindex 一份空内容文件（触发 delete 后 0 chunk）4. 再 GET /rag/documents | 步骤 2 存在该 document_id；步骤 4 该 document_id 不再出现（chunk 已全删）；列表接口不返回该项 | 无 | 已实现 | routes.py:148-163 从 chunk metadata 反推文档，无 chunk 即无文档；删除入口 pipeline/__init__.py:161 + vector_store.py:449-456 |
| TC-RAG-RET-071 | 一致性 | 入库后检索命中、删除后检索不命中 | P0 | 一致性 | allowed 目录内有含独特词 SentinelWord 的文件 | {"document_id":"doc-sentinel","file_path":"<allowed>/s.txt"} 与 {"query":"SentinelWord","top_k":5} | 1. POST /rag/documents/ingest 入库 2. POST /rag/search query=SentinelWord 断言 results 非空且 document_id 命中 3. reindex 该 document_id 指向不含该词的文件 4. 再 POST /rag/search query=SentinelWord | 步骤 2 results 非空且含该 document_id；步骤 4 results 恰为空数组；两步无 500 | 无 | 已实现 | 删除与写入顺序 pipeline/__init__.py:161-162；检索侧 status=active 过滤 vector_store.py:219-220 |
| TC-RAG-RET-072 | 一致性 | top_k 大于语料 chunk 总数时返回全部 | P1 | 边界 | 语料仅 3 个 chunk | {"query":"焊接","top_k":50} | 1. 使用仅 3 chunk 的语料 2. POST /rag/search top_k=50 3. 统计 results 长度 | HTTP 200；results 长度恰为 3（全部 chunk）；不重复、不返回空位、不报错 | 无 | 已实现 | vector_store.py:208 `n_results=min(n_results, col.count())`；vector_store.py:226 `return hits[:top_k]` |
| TC-RAG-RET-073 | 一致性 | 混合候选 RRF 按 chunk_id 去重且向量命中优先 | P1 | 一致性 | 开启 RAG_HYBRID_CANDIDATES（默认 true） | 同一 chunk 同时被向量路与词法路命中 | 1. 构造向量候选含 chunk X、词法候选也含 chunk X 的场景 2. 断言结果中 chunk X 只出现一次 3. 断言其 score 高于纯词法候选（score=0） | 结果中 chunk_id 唯一无重复；同 chunk 的命中对象保留向量路版本；纯词法候选 score 为 0.0 | rag/tests/test_hybrid_candidates.py::test_rrf_merges_and_dedupes_by_chunk_id、::test_rrf_keeps_vector_hit_for_duplicate_chunk、::test_rrf_respects_top_k、::test_rrf_with_empty_lexical_returns_vector_head（纯函数全覆盖） | 已实现 | retrieve.py:386-404 `_rrf_fuse_candidates` 以 chunk_id 累计 1/(k+pos) 并按分排序，preferred 表保证 prefer_vector 覆盖；词法候选 score 置 0 见 retrieve.py:461 |
| TC-RAG-RET-074 | 检索层 | 术语扩展在 embedding 前生效 | P1 | 正向 | 语料含"开路电压"，术语表含 OCV→开路电压 | {"query":"OCV异常怎么处理？","top_k":5} | 1. POST /rag/search query 含缩写 OCV 2. 断言 expanded_query 含中文全称 3. 断言 term_expansion_hits 非空且 canonical_term 为 "OCV" 4. 断言 expanded_query 不等于请求 query | HTTP 200；expanded_query 含 "开路电压"；term_expansion_hits[0].canonical_term 恰为 "OCV"；SearchResult.expanded_query 与请求 query 不同 | rag/tests/test_retrieval_quality.py::test_search_expands_terminology_before_embedding_and_records_hits | 已实现 | pipeline/__init__.py:200-202 `expand_query_with_terms` 后 `embed_query(retrieval_query)`；routes.py:338 传原始 query、结果里回填 expanded_query（pipeline/__init__.py:243） |
| TC-RAG-RET-075 | 检索层 | 非注册扩展名之外的图片格式无法解析（OCR 缺失） | P1 | 异常 | allowed 目录内有 .png 文件 | {"document_id":"doc-img","file_path":"<allowed>/a.png"} | 1. 在 allowed 目录放入一张 .png 2. POST /rag/documents/ingest 3. 读取状态码与 detail | 期望支持图片解析或明确拒绝；实际 HTTP 400，detail 为 "No parser registered for extension: .png"（OCR 链路整体缺失） | 无 | 未发现实现 | parsers 目录仅 base/docx/html/markdown/pdf/text 六个解析器；注册表 pipeline/__init__.py:55-92 无图片扩展名；全仓 grep ocr/paddleocr 无命中 |
| TC-RAG-RET-076 | 检索层 | 入库时 chunk 携带 status=active 与默认密级 | P1 | 正向 | allowed 目录内有文件 | {"document_id":"doc-active","file_path":"<allowed>/a.txt"} | 1. POST /rag/documents/ingest 2. POST /rag/search 命中该文档 3. 读取命中项 metadata.status 与 metadata.security_level | HTTP 200；metadata.status 恰为 "active"；未在 metadata 指定密级时 metadata.security_level 恰为 "internal" | 无 | 已实现 | pipeline/__init__.py:142-155 逐 chunk 写入 status="active" 与 security_level 默认值；检索侧再过滤一次 vector_store.py:219-220 |
| TC-RAG-RET-077 | 检索层 | token_count 与 offset 区间随 chunk 落库 | P2 | 正向 | allowed 目录内有较长文档 | {"document_id":"doc-offset","file_path":"<allowed>/a.txt"} | 1. 入库 2. 检索命中 3. 读取命中项 offset_start/offset_end 与 metadata.offset_unit | HTTP 200；offset_start 为整数且 offset_end > offset_start；metadata.offset_unit 恰为 "char" | rag/tests/test_source_metadata.py::test_build_source_metadata_marks_unavailable_ranges（覆盖 SourceMetadata 层，不覆盖 chunker 产出） | 已实现 | chunker.py:167-168 从段落 start/end 取偏移；chunker.py:189 `"offset_unit": "char"`；落库 vector_store.py:142-144 |
| TC-RAG-RET-078 | 检索层 | 空 content 的命中不进入结果（status 过滤旁路） | P2 | 边界 | 手工把某 chunk 的 metadata.status 改为 deleted | 该 chunk 的 metadata.status="deleted" | 1. 入库一篇文档 2. 直接在 Chroma 中把其中一个 chunk 的 status 改成 deleted 3. POST /rag/search 该文档内容 4. 断言该 chunk_id 不在 results 中 | HTTP 200；被标记 deleted 的 chunk_id 不出现在 results 中，同文档其余 chunk 仍可命中 | 无 | 已实现 | vector_store.py:219-220 `if metadata.get("status", "active") != "active": continue`（Python 侧过滤，非 Chroma where）；词法路同样过滤 vector_store.py:297-298 |
